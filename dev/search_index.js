var documenterSearchIndex = {"docs":
[{"location":"features/multi-threading/#Multi-Threading-Guide","page":"Multi-threading","title":"Multi-Threading Guide","text":"AdaptiveArrayPools uses task_local_storage() for task-local isolation: each Julia Task gets its own independent pool. This design ensures thread safety when used correctly.","category":"section"},{"location":"features/multi-threading/#Table-of-Contents","page":"Multi-threading","title":"Table of Contents","text":"Understanding Julia's Task/Thread Model\nHow Pools Work with @threads\nSafe Patterns\nUnsafe Patterns\nWhy Task-Local (Not Thread-Local)?\nUser Responsibility\n\n","category":"section"},{"location":"features/multi-threading/#Understanding-Julia's-Task/Thread-Model","page":"Multi-threading","title":"Understanding Julia's Task/Thread Model","text":"Julia uses an M:N threading model where multiple Tasks (lightweight coroutines) can run on multiple OS threads.\n\n┌─────────────────────────────────────────────────────────────┐\n│                     Julia Process                            │\n│                                                              │\n│  Thread 1              Thread 2              Thread 3        │\n│  ┌─────────┐          ┌─────────┐          ┌─────────┐      │\n│  │ Task A  │          │ Task C  │          │ Task E  │      │\n│  │ (TLS-A) │          │ (TLS-C) │          │ (TLS-E) │      │\n│  └─────────┘          └─────────┘          └─────────┘      │\n│  ┌─────────┐          ┌─────────┐                           │\n│  │ Task B  │          │ Task D  │                           │\n│  │ (TLS-B) │          │ (TLS-D) │                           │\n│  └─────────┘          └─────────┘                           │\n└─────────────────────────────────────────────────────────────┘\n\nKey concepts:\n\nConcept Description\nThread OS-level execution unit. Fixed count at Julia startup.\nTask Julia's lightweight coroutine (Green Thread). Created dynamically.\ntasklocalstorage() Per-Task storage. Each Task has its own isolated TLS.","category":"section"},{"location":"features/multi-threading/#Important:-One-Thread-Can-Run-Multiple-Tasks","page":"Multi-threading","title":"Important: One Thread Can Run Multiple Tasks","text":"A single thread can execute multiple Tasks by switching between them at yield points (I/O, sleep(), yield(), etc.):\n\n# Both tasks run on Thread 1, interleaved!\ntask_a = @spawn begin\n    println(\"A start\")\n    sleep(0.1)      # yield point - switch to Task B\n    println(\"A end\")\nend\n\ntask_b = @spawn begin\n    println(\"B start\")\n    sleep(0.1)      # yield point - switch back to Task A\n    println(\"B end\")\nend\n\n# Output (single thread):\n# A start\n# B start\n# A end\n# B end\n\n","category":"section"},{"location":"features/multi-threading/#How-Pools-Work-with-@threads","page":"Multi-threading","title":"How Pools Work with @threads","text":"When you use Threads.@threads, Julia distributes iterations across threads. Each thread gets one Task that processes its assigned iterations.\n\nThreads.@threads for i in 1:100_000   (4 threads)\n│\n├─ Thread 1: Task-1 → Pool-1\n│   └─ Processes i = 1..25,000 (same pool reused for all!)\n│\n├─ Thread 2: Task-2 → Pool-2\n│   └─ Processes i = 25,001..50,000\n│\n├─ Thread 3: Task-3 → Pool-3\n│   └─ Processes i = 50,001..75,000\n│\n└─ Thread 4: Task-4 → Pool-4\n    └─ Processes i = 75,001..100,000\n\nTotal: 4 pools created, each reused ~25,000 times","category":"section"},{"location":"features/multi-threading/#Key-Insight","page":"Multi-threading","title":"Key Insight","text":"@threads creates one Task per thread (not one per iteration!)\nEach Task has its own task_local_storage() → its own pool\nWithin one @threads block, pools are efficiently reused\nCalling @threads multiple times creates new Tasks → new pools each time\n\n","category":"section"},{"location":"features/multi-threading/#Safe-Patterns","page":"Multi-threading","title":"Safe Patterns","text":"","category":"section"},{"location":"features/multi-threading/#Pattern-1:-@with_pool-Inside-@threads","page":"Multi-threading","title":"Pattern 1: @with_pool Inside @threads","text":"Threads.@threads for i in 1:N\n    @with_pool pool begin\n        a = acquire!(pool, Float64, 100)\n        # ... computation ...\n    end  # pool automatically rewinds\nend\n\nEach thread's Task gets its own pool. Safe and efficient.","category":"section"},{"location":"features/multi-threading/#Pattern-2:-Function-Defined-with-@with_pool","page":"Multi-threading","title":"Pattern 2: Function Defined with @with_pool","text":"# Define function with @with_pool\n@with_pool pool function inner_work(x)\n    tmp = acquire!(pool, Float64, length(x))\n    tmp .= x\n    return sum(tmp)\nend\n\n# Call from @threads - each thread gets its own pool\nThreads.@threads for i in 1:N\n    result = inner_work(data[i])\nend\n\nThe pool is created per-Task when the function is called, not when defined.","category":"section"},{"location":"features/multi-threading/#Pattern-3:-Nested-Functions","page":"Multi-threading","title":"Pattern 3: Nested Functions","text":"@with_pool outer_pool function outer_work(data)\n    # outer_pool belongs to Main Task\n    tmp = acquire!(outer_pool, Float64, 100)\n\n    Threads.@threads for i in 1:length(data)\n        # inner_work creates its own pool per thread\n        inner_work(data[i])  # Inner pool ≠ outer_pool (safe!)\n    end\nend\n\nOuter and inner pools are completely independent.\n\n","category":"section"},{"location":"features/multi-threading/#Unsafe-Patterns","page":"Multi-threading","title":"Unsafe Patterns","text":"","category":"section"},{"location":"features/multi-threading/#Pattern-1:-@with_pool-Outside-@threads","page":"Multi-threading","title":"Pattern 1: @with_pool Outside @threads","text":"# ❌ DANGER: Race condition!\n@with_pool pool Threads.@threads for i in 1:N\n    a = acquire!(pool, Float64, 100)  # All threads share ONE pool!\nend\n\nWhy it fails: pool is created in the Main Task's TLS. All threads access the same pool simultaneously.","category":"section"},{"location":"features/multi-threading/#Pattern-2:-Sharing-Pool-Reference","page":"Multi-threading","title":"Pattern 2: Sharing Pool Reference","text":"# ❌ DANGER: Race condition!\npool = get_task_local_pool()  # Main Task's pool\nThreads.@threads for i in 1:N\n    a = acquire!(pool, Float64, 100)  # Shared access!\nend","category":"section"},{"location":"features/multi-threading/#Pattern-3:-Passing-Pool-to-@spawn","page":"Multi-threading","title":"Pattern 3: Passing Pool to @spawn","text":"# ❌ DANGER: Race condition!\n@with_pool pool begin\n    tasks = [Threads.@spawn begin\n        a = acquire!(pool, Float64, 100)  # Multiple tasks, one pool!\n    end for _ in 1:4]\n    wait.(tasks)\nend\n\n","category":"section"},{"location":"features/multi-threading/#Why-Task-Local-(Not-Thread-Local)?","page":"Multi-threading","title":"Why Task-Local (Not Thread-Local)?","text":"You might wonder: \"Why not use thread-local pools? They persist across @threads calls!\"","category":"section"},{"location":"features/multi-threading/#The-Stack-Discipline-Problem","page":"Multi-threading","title":"The Stack Discipline Problem","text":"AdaptiveArrayPools uses checkpoint! and rewind! - a stack-based allocation system:\n\n@with_pool pool begin\n    checkpoint!(pool)  # Push current state\n    a = acquire!(pool, ...)\n    b = acquire!(pool, ...)\n    # ...\n    rewind!(pool)      # Pop and restore state (LIFO!)\nend\n\nThis requires strict LIFO ordering: the Task that checkpoints first must rewind last.","category":"section"},{"location":"features/multi-threading/#Why-Thread-Local-Fails-with-@spawn","page":"Multi-threading","title":"Why Thread-Local Fails with @spawn","text":"With @spawn, multiple Tasks can interleave on the same thread:\n\nThread 1 (with Thread-Local Pool):\n\nTime →\nTask A: checkpoint! ──── acquire! ──── sleep ────────────── rewind!\nTask B:        checkpoint! ──── acquire! ──── sleep ──── rewind!\n                                                    ↑\n                                           A finishes first!\n\nStack corruption occurs:\n\nTask A: checkpoint! → stack = [0]\nTask B: checkpoint! → stack = [0, 1]\nTask A: rewind! → pops 1 (B's checkpoint!) → stack = [0]\nTask B: rewind! → pops 0 (A's checkpoint!) → WRONG!\n\nResult: B's arrays may be reused while B is still using them → memory corruption.","category":"section"},{"location":"features/multi-threading/#Locks-Don't-Help","page":"Multi-threading","title":"Locks Don't Help","text":"Adding locks only prevents simultaneous access, not LIFO violations. The stack still gets corrupted because Tasks finish in unpredictable order.","category":"section"},{"location":"features/multi-threading/#Task-Local:-The-Only-Safe-Solution","page":"Multi-threading","title":"Task-Local: The Only Safe Solution","text":"With Task-local pools:\n\nEach Task has its own pool\nEach pool has its own stack\nNo interleaving possible → LIFO always preserved\n\n","category":"section"},{"location":"features/multi-threading/#User-Responsibility","page":"Multi-threading","title":"User Responsibility","text":"","category":"section"},{"location":"features/multi-threading/#The-Core-Rule","page":"Multi-threading","title":"The Core Rule","text":"Pool objects must not be shared across Tasks.\n\nThis library prioritizes zero-overhead performance over runtime safety checks. No locks are added because:\n\nLocks would defeat the purpose of zero-allocation pooling\nEven with locks, stack corruption would occur (LIFO violations)","category":"section"},{"location":"features/multi-threading/#Quick-Reference","page":"Multi-threading","title":"Quick Reference","text":"Pattern Safety Reason\n@with_pool inside @threads ✅ Safe Each Task gets own pool\n@with_pool outside @threads ❌ Unsafe All threads share one pool\nFunction with @with_pool called from @threads ✅ Safe Pool created per-Task at call time\nPassing pool to @spawn ❌ Unsafe Multiple Tasks access same pool\nNested @with_pool (outer/inner) ✅ Safe Each level has independent pool","category":"section"},{"location":"features/multi-threading/#Debugging-Tips","page":"Multi-threading","title":"Debugging Tips","text":"If you encounter unexpected behavior:\n\nCheck pool placement: Is @with_pool inside or outside @threads?\nCheck pool sharing: Is the same pool variable accessed from multiple Tasks?\nEnable POOL_DEBUG: POOL_DEBUG[] = true catches some (not all) misuse patterns\n\n","category":"section"},{"location":"features/multi-threading/#Summary","page":"Multi-threading","title":"Summary","text":"AdaptiveArrayPools uses Task-local isolation for thread safety\nEach Julia Task gets its own independent pool via task_local_storage()\n@threads creates one Task per thread → pools are reused within the block\nAlways place @with_pool inside @threads, not outside\nThread-local pools are not an alternative due to stack discipline requirements\nCorrect usage is the user's responsibility (no runtime checks for performance)","category":"section"},{"location":"advanced/pool-patterns/#Advanced-Pool-Patterns","page":"Pool Patterns","title":"Advanced Pool Patterns","text":"This page covers advanced usage patterns for experienced users.","category":"section"},{"location":"advanced/pool-patterns/#Calling-Other-@with_pool-Functions","page":"Pool Patterns","title":"Calling Other @with_pool Functions","text":"Each @with_pool function manages its own checkpoint. They can call each other freely:\n\n@with_pool pool function step1(n)\n    A = zeros!(pool, Float64, n)\n    fill!(A, 1.0)\n    return sum(A)\nend\n\n@with_pool pool function step2(n)\n    B = zeros!(pool, Float64, n)\n    fill!(B, 2.0)\n    return sum(B)\nend\n\n@with_pool pool function pipeline(n)\n    a = step1(n)   # step1's arrays marked for reuse when it returns\n    b = step2(n)   # step2's arrays marked for reuse when it returns\n    C = acquire!(pool, Float64, n)\n    fill!(C, a + b)\n    return sum(C)\nend","category":"section"},{"location":"advanced/pool-patterns/#Passing-Pool-as-Argument","page":"Pool Patterns","title":"Passing Pool as Argument","text":"For complex call hierarchies, use @with_pool only at the top level and pass the pool through function arguments:\n\n# Inner functions receive pool as argument - no @with_pool needed\nfunction compute_step!(pool, data, result)\n    temp = acquire!(pool, Float64, length(data))\n    temp .= data .* 2\n    result[] += sum(temp)\nend\n\nfunction process_chunk!(pool, chunk, result)\n    temp = zeros!(pool, Float64, length(chunk))\n    compute_step!(pool, chunk, temp)\n    result[] += sum(temp)\nend\n\n# Only the entry point uses @with_pool\n@with_pool pool function main_computation(chunks)\n    result = Ref(0.0)\n    for chunk in chunks\n        process_chunk!(pool, chunk, result)\n    end\n    return result[]\nend\n\nBenefits:\n\nSingle checkpoint/rewind at top level\nInner functions are simpler (no macro overhead)\nPool lifetime is explicit and controlled","category":"section"},{"location":"advanced/pool-patterns/#Direct-Pool-Access-in-Inner-Functions","page":"Pool Patterns","title":"Direct Pool Access in Inner Functions","text":"An alternative to passing pool as argument: inner functions call get_task_local_pool() directly, while a top-level @with_pool function controls the lifecycle.\n\n# Inner functions access pool directly - no argument needed\nfunction compute_step!(data, result)\n    pool = get_task_local_pool()  # Direct access\n    temp = acquire!(pool, Float64, length(data))\n    temp .= data .* 2\n    result[] += sum(temp)\n    # temp NOT released here - stays active\nend\n\nfunction process_chunk!(chunk, accumulator)\n    pool = get_task_local_pool()  # Direct access\n    buffer = zeros!(pool, Float64, length(chunk))\n    compute_step!(chunk, buffer)\n    accumulator[] += sum(buffer)\n    # buffer NOT released here - stays active\nend\n\n# Top-level controls lifecycle with @with_pool\n@with_pool pool function main_pipeline(chunks)\n    #  checkpoint!() ─────────────────────────────────┐\n    accumulator = Ref(0.0)                          # │\n    for chunk in chunks                             # │\n        process_chunk!(chunk, accumulator)          # │  All arrays from\n        # └─ compute_step! allocates temp           # │  inner functions\n        # └─ process_chunk! allocates buffer        # │  accumulate here\n    end                                             # │\n    return accumulator[]                            # │\n    #  rewind!() ─────────────────────────────────────┘\n    #     └─ ALL arrays (temp, buffer, ...) marked for reuse\nend","category":"section"},{"location":"advanced/pool-patterns/#Memory-Flow-Visualization","page":"Pool Patterns","title":"Memory Flow Visualization","text":"main_pipeline(chunks)          Inner Functions\n       │\n  checkpoint!()\n       │\n       ├──► process_chunk!()\n       │         │\n       │         ├──► get_task_local_pool() ──► buffer allocated\n       │         │\n       │         └──► compute_step!()\n       │                   │\n       │                   └──► get_task_local_pool() ──► temp allocated\n       │\n       ├──► process_chunk!()  (next iteration)\n       │         └──► ... more allocations ...\n       │\n       ▼\n    rewind!()  ◄─────── ALL arrays marked for reuse","category":"section"},{"location":"advanced/pool-patterns/#User-Responsibility-Warning","page":"Pool Patterns","title":"⚠️ User Responsibility Warning","text":"This pattern requires you to guarantee that inner functions are always called through a @with_pool entry point:\n\n# SAFE: Called through main_pipeline\nmain_pipeline(my_chunks)  # ✓ Lifecycle managed\n\n# DANGEROUS: Direct call without @with_pool wrapper\ncompute_step!(some_data, some_ref)  # ✗ No checkpoint/rewind!\n# └─ Arrays allocated but NEVER marked for reuse → pool grows unboundedly\n\nWhen to use this pattern:\n\nDeep call hierarchies where threading pool through every function is tedious\nPerformance-critical code where you want to avoid argument passing overhead\nYou can enforce that all entry points use @with_pool\n\nWhen to prefer \"Passing Pool as Argument\":\n\nFunctions may be called from various contexts (some pooled, some not)\nLibrary code where you can't control the caller\nYou want explicit documentation of pool dependency in function signatures","category":"section"},{"location":"advanced/pool-patterns/#Manual-Checkpoint/Rewind","page":"Pool Patterns","title":"Manual Checkpoint/Rewind","text":"For fine-grained control, use checkpoint! and rewind! directly:\n\nfunction manual_control()\n    pool = get_task_local_pool()\n\n    checkpoint!(pool)\n    try\n        A = acquire!(pool, Float64, 100)\n        B = acquire!(pool, Float64, 100)\n        # ... compute ...\n        return sum(A) + sum(B)\n    finally\n        rewind!(pool)\n    end\nend\n\nThis is what @with_pool generates internally. Use manual control when:\n\nIntegrating with existing try/catch blocks\nConditional checkpoint/rewind logic needed\nBuilding custom pool management abstractions","category":"section"},{"location":"advanced/pool-patterns/#Scope-Only-@with_pool","page":"Pool Patterns","title":"Scope-Only @with_pool","text":"You can omit the pool name when inner functions handle their own acquire:\n\n@with_pool p function step1()\n    v = acquire!(p, Float64, 100)\n    sum(v)\nend\n\n@with_pool p function step2()\n    v = acquire!(p, Float64, 200)\n    sum(v)\nend\n\n# Outer function just provides scope management\n@with_pool function orchestrate()\n    a = step1()\n    b = step2()\n    return a + b\nend\n\nThe name-less @with_pool still performs checkpoint/rewind but doesn't expose the pool variable. This is useful when you're orchestrating other @with_pool functions.","category":"section"},{"location":"advanced/pool-patterns/#See-Also","page":"Pool Patterns","title":"See Also","text":"@with_pool Patterns - Basic usage patterns\nSafety Rules - Scope rules","category":"section"},{"location":"architecture/how-it-works/#How-It-Works","page":"How It Works","title":"How It Works","text":"This page explains the core mechanisms that enable zero-allocation array reuse.","category":"section"},{"location":"architecture/how-it-works/#The-Zero-Allocation-Promise","page":"How It Works","title":"The Zero-Allocation Promise","text":"+-------------------------------------------------------------+\n|  Call 1 (warmup):                                           |\n|    checkpoint! --> acquire! x 3 --> rewind!                 |\n|         |                                                   |\n|         +-- backing memory allocated                        |\n|                                                             |\n|  Call 2+ (zero-alloc):                                      |\n|    checkpoint! --> acquire! x 3 --> rewind!                 |\n|         |                                                   |\n|         +-- same memory reused, 0 bytes allocated           |\n+-------------------------------------------------------------+","category":"section"},{"location":"architecture/how-it-works/#Checkpoint/Rewind-Lifecycle","page":"How It Works","title":"Checkpoint/Rewind Lifecycle","text":"The core mechanism that enables memory reuse:\n\n@with_pool pool function foo()\n    |\n    +---> checkpoint!(pool)     # Save current state (n_active counters)\n    |\n    |     A = acquire!(pool, ...)  # n_active += 1\n    |     B = acquire!(pool, ...)  # n_active += 1\n    |     C = acquire!(pool, ...)  # n_active += 1\n    |     ... compute ...\n    |\n    +---> rewind!(pool)         # Restore n_active, arrays available for reuse\nend\n\nOn repeated calls, the same memory is reused without any allocation.","category":"section"},{"location":"architecture/how-it-works/#Exception-Safety:-try...finally","page":"How It Works","title":"Exception Safety: try...finally","text":"The @with_pool macro generates code with exception-safe cleanup:\n\n# What you write:\n@with_pool pool begin\n    A = acquire!(pool, Float64, 100)\n    result = compute(A)\nend\n\n# What the macro generates:\nlet pool = get_task_local_pool()\n    checkpoint!(pool)\n    try\n        A = acquire!(pool, Float64, 100)\n        result = compute(A)\n    finally\n        rewind!(pool)  # Always executes, even on exception\n    end\nend\n\nKey guarantee: The finally block ensures rewind! is called even if an exception occurs, preventing memory leaks and state corruption.","category":"section"},{"location":"architecture/how-it-works/#Fixed-Slot-Type-Dispatch","page":"How It Works","title":"Fixed-Slot Type Dispatch","text":"To achieve zero-lookup overhead, common types have dedicated struct fields:\n\nstruct AdaptiveArrayPool\n    float64::TypedPool{Float64}\n    float32::TypedPool{Float32}\n    int64::TypedPool{Int64}\n    int32::TypedPool{Int32}\n    complexf64::TypedPool{ComplexF64}\n    complexf32::TypedPool{ComplexF32}\n    bool::TypedPool{Bool}\n    others::IdDict{DataType, Any}  # Fallback for rare types\nend\n\nWhen you call acquire!(pool, Float64, n), the compiler inlines directly to pool.float64 — no dictionary lookup, no type instability.","category":"section"},{"location":"architecture/how-it-works/#N-D-Wrapper-Reuse-(CPU)","page":"How It Works","title":"N-D Wrapper Reuse (CPU)","text":"For unsafe_acquire! (which returns native Array types), the caching strategy depends on the Julia version:","category":"section"},{"location":"architecture/how-it-works/#Julia-1.11:-setfield!-based-Wrapper-Reuse-(Zero-Allocation)","page":"How It Works","title":"Julia 1.11+: setfield!-based Wrapper Reuse (Zero-Allocation)","text":"Julia 1.11 changed Array from an opaque C struct to a mutable Julia struct with ref::MemoryRef{T} and size::NTuple{N,Int} fields. This enables in-place mutation of cached Array wrappers via setfield!:\n\nnd_wrappers[N][slot] → cached Array{T,N}\n    │\n    ├─ setfield!(:ref, new_memory_ref)   ← update backing memory (0-alloc)\n    └─ setfield!(:size, new_dims)        ← update dimensions (0-alloc)\n\nResult: Unlimited dimension patterns per slot with zero allocation after warmup. No eviction, no round-robin, no CACHE_WAYS limit.\n\n# Pseudocode for Julia 1.11+ path\nfunction unsafe_acquire!(pool, T, dims...)\n    typed_pool = get_typed_pool!(pool, T)\n    flat_view = get_view!(typed_pool, prod(dims))\n    slot = typed_pool.n_active\n\n    # Direct index lookup by dimensionality N (~1ns)\n    wrapper = typed_pool.nd_wrappers[N][slot]\n    if wrapper !== nothing\n        setfield!(wrapper, :ref, getfield(vec, :ref))  # 0-alloc\n        setfield!(wrapper, :size, dims)                 # 0-alloc\n        return wrapper\n    end\n\n    # First call for this (slot, N): unsafe_wrap once, cached forever\n    arr = wrap_array(typed_pool, flat_view, dims)\n    store_wrapper!(typed_pool, N, slot, arr)\n    return arr\nend","category":"section"},{"location":"architecture/how-it-works/#Julia-1.10-(Legacy):-N-Way-Set-Associative-Cache","page":"How It Works","title":"Julia 1.10 (Legacy): N-Way Set Associative Cache","text":"On Julia 1.10, Array fields cannot be mutated, so the legacy path uses a 4-way set-associative cache with round-robin eviction:\n\nCache hit (≤CACHE_WAYS dimension patterns per slot): 0 bytes\nCache miss (>CACHE_WAYS patterns): ~80-144 bytes per unsafe_wrap call\n\nSee Configuration for CACHE_WAYS tuning (Julia 1.10 / CUDA only).","category":"section"},{"location":"architecture/how-it-works/#CUDA:-N-Way-Cache","page":"How It Works","title":"CUDA: N-Way Cache","text":"The CUDA backend still uses the N-way set-associative cache (same as Julia 1.10 legacy), since CuArray does not support setfield!-based mutation.","category":"section"},{"location":"architecture/how-it-works/#View-vs-Array-Return-Types","page":"How It Works","title":"View vs Array Return Types","text":"Type stability is critical for performance. AdaptiveArrayPools provides two APIs:\n\nAPI 1D Return N-D Return Allocation (Julia 1.11+) Allocation (Julia 1.10 / CUDA)\nacquire! SubArray{T,1} ReshapedArray{T,N} Always 0 bytes Always 0 bytes\nunsafe_acquire! Vector{T} Array{T,N} 0 bytes (setfield! reuse) 0 bytes (hit) / ~100 bytes (miss)\n\nnote: `Bit` type behavior\nFor T === Bit, both acquire! and unsafe_acquire! return native BitVector / BitArray{N} (not views). Cache hit achieves 0 bytes allocation.","category":"section"},{"location":"architecture/how-it-works/#Why-Two-APIs?","page":"How It Works","title":"Why Two APIs?","text":"acquire! (views) — The compiler can eliminate view wrappers entirely through SROA (Scalar Replacement of Aggregates) and escape analysis. This is why 1D SubArray and N-D ReshapedArray achieve true zero allocation.\n\nunsafe_acquire! (arrays) — Sometimes you need a concrete Array type:\n\nFFI/C interop requiring Ptr{T} from contiguous memory\nType signatures that explicitly require Array{T,N}\nAvoiding runtime dispatch in polymorphic code","category":"section"},{"location":"architecture/how-it-works/#Typed-Checkpoint/Rewind-Optimization","page":"How It Works","title":"Typed Checkpoint/Rewind Optimization","text":"When the @with_pool macro can statically determine which types are used, it generates optimized code:\n\n# If only Float64 is used in the block:\ncheckpoint!(pool, Float64)  # ~77% faster than full checkpoint\n# ... compute ...\nrewind!(pool, Float64)\n\nThis avoids iterating over all type slots and the others IdDict.","category":"section"},{"location":"architecture/how-it-works/#1-Based-Sentinel-Pattern","page":"How It Works","title":"1-Based Sentinel Pattern","text":"Internal state vectors use a sentinel at index 0 to eliminate isempty() checks:\n\n_checkpoint_n_active = [0]  # Sentinel at depth=0\n_checkpoint_depths = [0]    # Global scope marker\n\nThis pattern reduces branching in hot paths where every nanosecond counts.","category":"section"},{"location":"architecture/how-it-works/#Further-Reading","page":"How It Works","title":"Further Reading","text":"For detailed design documents:\n\nhybrid_api_design.md — Two-API strategy (acquire! vs unsafe_acquire!) and type stability analysis\nnd_array_approach_comparison.md — N-way cache design, boxing analysis, and ReshapedArray benchmarks\nuntracked_acquire_design.md — Macro-based untracked acquire detection and 1-based sentinel pattern\nfixed_slots_codegen_design.md — Zero-allocation iteration via @generated functions\ncuda_extension_design.md — CUDA backend architecture and extension loading","category":"section"},{"location":"basics/safety-rules/#Safety-Rules","page":"Safety Rules","title":"Safety Rules","text":"AdaptiveArrayPools achieves zero allocation by reusing memory across calls. This requires understanding one critical rule.\n\n","category":"section"},{"location":"basics/safety-rules/#The-One-Rule","page":"Safety Rules","title":"The One Rule","text":"+-------------------------------------------------------------+\n|                                                             |\n|  Pool arrays are ONLY valid within their @with_pool scope   |\n|                                                             |\n|  When the scope ends, arrays are marked for reuse.          |\n|  Using arrays after scope ends = UNDEFINED BEHAVIOR         |\n|                                                             |\n+-------------------------------------------------------------+","category":"section"},{"location":"basics/safety-rules/#What's-Safe","page":"Safety Rules","title":"What's Safe","text":"Pattern Example Why It Works\nReturn computed values return sum(v) Scalar escapes, not the array\nReturn copies return copy(v) New allocation, independent data\nUse within scope result = A * B Arrays valid during computation","category":"section"},{"location":"basics/safety-rules/#What's-Dangerous","page":"Safety Rules","title":"What's Dangerous","text":"Pattern Example Why It Fails\nReturn array return v Array marked for reuse after return\nStore in global global_ref = v Points to reusable memory\nCapture in closure () -> sum(v) v may be overwritten when closure runs\n\n","category":"section"},{"location":"basics/safety-rules/#The-Scope-Rule-in-Detail","page":"Safety Rules","title":"The Scope Rule in Detail","text":"When @with_pool ends, all arrays acquired within that scope are marked available for reuse—not immediately freed. This is what makes zero-allocation possible on subsequent calls.\n\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)\n\n    result = sum(v)  # ✅ compute and return values\n    copied = copy(v) # ✅ copy if you need data outside\nend\n# v is no longer valid here - it's marked for reuse\n\nwarning: Why Undefined Behavior?\nAfter scope ends, using v is undefined because:Subsequent acquire! calls may overwrite the data — the memory is available for reuse\nTask termination may trigger GC — the pool itself could be garbage collected\nIt might \"work\" by luck — data unchanged until next acquire, but don't rely on thisThe worst case is silent data corruption: your code appears to work but produces wrong results intermittently.","category":"section"},{"location":"basics/safety-rules/#What-NOT-to-Do","page":"Safety Rules","title":"What NOT to Do","text":"","category":"section"},{"location":"basics/safety-rules/#Don't-return-pool-backed-arrays","page":"Safety Rules","title":"Don't return pool-backed arrays","text":"# ❌ Wrong: returning the array itself\n@with_pool pool function bad_example()\n    v = acquire!(pool, Float64, 100)\n    return v  # v marked for reuse after return!\nend\n\n# ✅ Correct: return computed values or copies\n@with_pool pool function good_example()\n    v = acquire!(pool, Float64, 100)\n    return sum(v)  # scalar result\nend","category":"section"},{"location":"basics/safety-rules/#Don't-store-in-globals-or-closures","page":"Safety Rules","title":"Don't store in globals or closures","text":"# ❌ Wrong: storing in global\nglobal_ref = nothing\n@with_pool pool begin\n    global_ref = acquire!(pool, Float64, 100)\nend\n# global_ref now points to reusable memory - data may be overwritten\n\n# ❌ Wrong: capturing in closure\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)\n    callback = () -> sum(v)  # v captured but may be overwritten later\nend","category":"section"},{"location":"basics/safety-rules/#Don't-resize-or-push!-to-unsafe_acquire!-arrays","page":"Safety Rules","title":"Don't resize or push! to unsafe_acquire! arrays","text":"@with_pool pool begin\n    v = unsafe_acquire!(pool, Float64, 100)\n    # ❌ These break pool memory management:\n    # resize!(v, 200)\n    # push!(v, 1.0)\n    # append!(v, [1.0, 2.0])\nend","category":"section"},{"location":"basics/safety-rules/#Debugging-with-POOL_DEBUG","page":"Safety Rules","title":"Debugging with POOL_DEBUG","text":"Enable runtime safety checks during development:\n\nusing AdaptiveArrayPools\nAdaptiveArrayPools.POOL_DEBUG[] = true\n\n@with_pool pool function test()\n    v = acquire!(pool, Float64, 100)\n    return v  # Will warn about returning pool-backed array\nend","category":"section"},{"location":"basics/safety-rules/#acquire!-vs-unsafe_acquire!","page":"Safety Rules","title":"acquire! vs unsafe_acquire!","text":"Function Returns Best For\nacquire! View types (SubArray, ReshapedArray) for most T; native BitVector/BitArray{N} for T === Bit General use, BLAS/LAPACK; fast bit masks\nunsafe_acquire! Native Array/CuArray (for T === Bit, equivalent to acquire!) FFI, type constraints\n\nBoth follow the same scope rules. Use acquire! by default—views work with all standard Julia linear algebra operations.","category":"section"},{"location":"basics/safety-rules/#Thread-Safety","page":"Safety Rules","title":"Thread Safety","text":"Pools are task-local, so each thread automatically gets its own pool:\n\n# ✅ Safe: each task has independent pool\nThreads.@threads for i in 1:N\n    @with_pool pool begin\n        a = acquire!(pool, Float64, 100)\n        # work with a...\n    end\nend\n\n# ❌ Unsafe: pool created outside threaded region\n@with_pool pool begin\n    Threads.@threads for i in 1:N\n        a = acquire!(pool, Float64, 100)  # race condition!\n    end\nend\n\nSee Multi-Threading for more patterns.","category":"section"},{"location":"basics/api-essentials/#Essential-API","page":"Essential API","title":"Essential API","text":"This page covers the core functions you'll use 99% of the time. For the complete API reference, see Full API.","category":"section"},{"location":"basics/api-essentials/#Array-Acquisition","page":"Essential API","title":"Array Acquisition","text":"","category":"section"},{"location":"basics/api-essentials/#acquire!(pool,-T,-dims...)","page":"Essential API","title":"acquire!(pool, T, dims...)","text":"The primary function. Returns a view (SubArray for 1D, ReshapedArray for N-D) for most T. For T === Bit, returns a native BitVector/BitArray{N}.\n\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)        # 1D: SubArray{Float64,1}\n    m = acquire!(pool, Float64, 10, 10)     # 2D: ReshapedArray{Float64,2}\n    t = acquire!(pool, Int64, 2, 3, 4)      # 3D: ReshapedArray{Int64,3}\n    mask = acquire!(pool, Bit, 1000)        # BitVector (bit-packed)\nend\n\nAlways use acquire! by default. Views are zero-allocation and work with BLAS/LAPACK; Bit masks are bit-packed and optimized for boolean-kernel operations (count, any, all, etc.).","category":"section"},{"location":"basics/api-essentials/#unsafe_acquire!(pool,-T,-dims...)","page":"Essential API","title":"unsafe_acquire!(pool, T, dims...)","text":"Returns a native Array type. On Julia 1.11+, always zero-allocation via setfield!-based wrapper reuse (unlimited dimension patterns). On Julia 1.10 and CUDA, zero-allocation on cache hit with a small header (~80-144 bytes) on cache miss. Use when you specifically need Array{T,N}:\n\n@with_pool pool begin\n    # Use when you need Array for:\n    arr = unsafe_acquire!(pool, Float64, 100)\n\n    # - FFI/ccall requiring Ptr{T}\n    ccall(:some_c_function, Cvoid, (Ptr{Float64}, Cint), arr, length(arr))\n\n    # - Functions with strict Array{T,N} type signatures\nend\n\ntip: Cache behavior\nOn Julia 1.11+: always 0 bytes regardless of dimension pattern (setfield!-based reuse). On Julia 1.10 / CUDA: same dimension pattern → 0 bytes, different pattern → 80-144 bytes header only (data always reused). See N-D Wrapper Caching for details.\n\nnote: `Bit` behavior\nFor T === Bit, unsafe_acquire! is equivalent to acquire! and returns native BitVector/BitArray{N}.","category":"section"},{"location":"basics/api-essentials/#Convenience-Functions","page":"Essential API","title":"Convenience Functions","text":"Zero-initialized arrays:\n\n@with_pool pool begin\n    z = zeros!(pool, Float64, 10, 10)   # All zeros\n    o = ones!(pool, Float64, 100)       # All ones\nend\n\nMatch existing array properties:\n\n@with_pool pool begin\n    A = acquire!(pool, Float64, 50, 50)\n    B = similar!(pool, A)                # Same type and size as A\n    C = similar!(pool, A, ComplexF64)    # Same size, different type\nend","category":"section"},{"location":"basics/api-essentials/#Reshaping-with-reshape!","page":"Essential API","title":"Reshaping with reshape!","text":"Reshape an existing array using the pool's wrapper cache. The result shares memory with the original — mutations are visible in both:\n\n@with_pool pool function process_grid(data, nx, ny)\n    M = reshape!(pool, data, nx, ny)   # 1D → 2D, shares memory with data\n    col_sums = zeros!(pool, Float64, ny)\n    for j in 1:ny, i in 1:nx\n        col_sums[j] += M[i, j]\n    end\n    return sum(col_sums)\nend\n\nOn Julia 1.11+, cross-dimensional reshapes are zero-allocation after warmup via setfield!-based wrapper reuse. On Julia 1.10, falls back to Base.reshape.\n\nwarning: DimensionMismatch\nprod(dims) must equal length(A), otherwise a DimensionMismatch is thrown.","category":"section"},{"location":"basics/api-essentials/#Custom-Initialization-with-fill!","page":"Essential API","title":"Custom Initialization with fill!","text":"For values other than 0 or 1, use Julia's built-in fill!:\n\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)\n    fill!(v, 3.14)              # Fill with pi\n\n    m = acquire!(pool, Int64, 10, 10)\n    fill!(m, -1)                # Fill with sentinel value\nend\n\nThis pattern works because pool arrays are mutable views into the underlying storage.","category":"section"},{"location":"basics/api-essentials/#Pool-Management","page":"Essential API","title":"Pool Management","text":"","category":"section"},{"location":"basics/api-essentials/#reset!(pool)","page":"Essential API","title":"reset!(pool)","text":"Releases all memory held by the pool. Useful for long-running processes:\n\n# After processing a large batch\n@with_pool pool begin\n    # ... large computation ...\nend\n\n# Optionally release memory if pool grew too large\nreset!(get_task_local_pool())","category":"section"},{"location":"basics/api-essentials/#pooling_enabled(pool)","page":"Essential API","title":"pooling_enabled(pool)","text":"Check if pooling is active (returns false for DisabledPool):\n\n@maybe_with_pool pool begin\n    if pooling_enabled(pool)\n        println(\"Using pool\")\n    else\n        println(\"Pooling disabled\")\n    end\nend","category":"section"},{"location":"basics/api-essentials/#Quick-Reference","page":"Essential API","title":"Quick Reference","text":"Function Returns Allocation Use Case\nacquire!(pool, T, dims...) View type 0 bytes Default choice\nunsafe_acquire!(pool, T, dims...) Array{T,N} 0 bytes (1.11+) / 0-144 (1.10/CUDA) FFI, type constraints\nzeros!(pool, [T,] dims...) View type 0 bytes Zero-initialized\nones!(pool, [T,] dims...) View type 0 bytes One-initialized\nsimilar!(pool, A) View type 0 bytes Match existing array\nreshape!(pool, A, dims...) Reshaped array 0 bytes (1.11+) Reshape sharing memory\nreset!(pool) nothing - Release all memory\npooling_enabled(pool) Bool - Check pool status","category":"section"},{"location":"basics/api-essentials/#See-Also","page":"Essential API","title":"See Also","text":"Full API Reference - Complete function list\n@with_pool Patterns - Usage patterns\nSafety Rules - Scope rules","category":"section"},{"location":"features/bit-arrays/#BitArray-Support","page":"Bit Arrays","title":"BitArray Support","text":"AdaptiveArrayPools.jl includes specialized support for BitArray (including BitVector and N-dimensional BitArray{N}), enabling ~8x memory savings for boolean arrays compared to standard Vector{Bool}.","category":"section"},{"location":"features/bit-arrays/#The-Bit-Sentinel-Type","page":"Bit Arrays","title":"The Bit Sentinel Type","text":"To distinguish between standard boolean arrays (Vector{Bool}, 1 byte/element) and bit-packed arrays (BitVector, 1 bit/element), use the Bit sentinel type.\n\nCall Result Memory\nacquire!(pool, Bool, 1000) Vector{Bool} 1000 bytes\nacquire!(pool, Bit, 1000) BitVector ~125 bytes","category":"section"},{"location":"features/bit-arrays/#Usage","page":"Bit Arrays","title":"Usage","text":"","category":"section"},{"location":"features/bit-arrays/#1D-Arrays-(BitVector)","page":"Bit Arrays","title":"1D Arrays (BitVector)","text":"For 1D arrays, acquire! returns a native BitVector backed by pooled storage. This design enables full SIMD optimization, making operations like count, any, and bitwise ops significantly faster than views.\n\n@with_pool pool begin\n    # Acquire a BitVector of length 1000\n    bv = acquire!(pool, Bit, 1000)\n\n    # Use like normal\n    bv .= true\n    bv[1] = false\n\n    # Supports standard operations with full SIMD acceleration\n    count(bv)\nend","category":"section"},{"location":"features/bit-arrays/#N-D-Arrays-(BitArray)","page":"Bit Arrays","title":"N-D Arrays (BitArray)","text":"For multi-dimensional arrays, acquire! returns a native BitArray{N} (e.g., BitMatrix for 2D) that shares chunks with pooled storage. This preserves the packed memory layout and SIMD benefits while providing N-D indexing.\n\n@with_pool pool begin\n    # 100x100 bit matrix (returns BitMatrix)\n    mask = zeros!(pool, Bit, 100, 100)\n\n    mask[5, 5] = true\n\n    # 3D BitArray\n    volume = acquire!(pool, Bit, 10, 10, 10)\nend","category":"section"},{"location":"features/bit-arrays/#Convenience-Functions","page":"Bit Arrays","title":"Convenience Functions","text":"For specific BitVector operations, prefer trues! and falses! which mirror Julia's standard functions:\n\n@with_pool pool begin\n    # Filled with false (equivalent to `falses(256)`)\n    mask = falses!(pool, 256)\n\n    # Filled with true (equivalent to `trues(256)`)\n    flags = trues!(pool, 256)\n\n    # Multidimensional\n    grid = trues!(pool, 100, 100)\n\n    # Similar to existing BitArray\n    A = BitVector(undef, 50)\n    B = similar!(pool, A)  # Reuses eltype(A) -> Bool\n\n    # To explicit get Bit-packed from pool irrespective of source\n    C = similar!(pool, A, Bit)\nend\n\nNote: zeros!(pool, Bit, ...) and ones!(pool, Bit, ...) are also supported (aliased to falses! and trues!).","category":"section"},{"location":"features/bit-arrays/#Performance-and-Safety","page":"Bit Arrays","title":"Performance & Safety","text":"","category":"section"},{"location":"features/bit-arrays/#Why-Native-BitArray?","page":"Bit Arrays","title":"Why Native BitArray?","text":"The pool returns native BitVector/BitArray types instead of SubArray views for performance. Operations like count(), sum(), and bitwise broadcasting are 10x~100x faster on native bit arrays because they utilize SIMD instructions on packed 64-bit chunks.","category":"section"},{"location":"features/bit-arrays/#N-D-Caching-and-Zero-Allocation","page":"Bit Arrays","title":"N-D Caching & Zero Allocation","text":"The pool reuses BitArray{N} wrapper instances via setfield!-based in-place mutation (Julia 1.11+) or N-way cache (Julia 1.10 / CUDA):\n\nScenario Julia 1.11+ Julia 1.10 / CUDA\nFirst call with new (slot, N) ~944 bytes (new BitArray{N}) ~944 bytes\nSubsequent call, any dims 0 bytes (setfield! reuse) 0 bytes (same ndims) / ~944 bytes (different ndims)\n\nOn Julia 1.11+, BitArray fields (len, dims, chunks) are mutated in-place via setfield!, achieving zero allocation on all repeated calls regardless of dimension pattern.\n\n@with_pool pool begin\n    # First call: allocates BitMatrix wrapper (~944 bytes)\n    m1 = acquire!(pool, Bit, 100, 100)\n\n    # Rewind to reuse the same slot\n    rewind!(pool)\n\n    # Same dims: 0 allocation (cached wrapper reused)\n    m2 = acquire!(pool, Bit, 100, 100)\n\n    rewind!(pool)\n\n    # Different dims but same ndims: 0 allocation (fields updated in-place)\n    m3 = acquire!(pool, Bit, 50, 200)\nend","category":"section"},{"location":"features/bit-arrays/#Important:-Do-Not-Resize","page":"Bit Arrays","title":"⚠️ Important: Do Not Resize","text":"While the returned arrays are standard BitVector types, they share their underlying memory chunks with the pool.\n\nwarning: Do Not Resize\nNEVER resize (push!, pop!, resize!) a pooled BitVector or BitArray.The underlying memory is owned and managed by the pool. Resizing it will detach it from the pool or potentially corrupt the shared state. Treat these arrays as fixed-size scratch buffers only.","category":"section"},{"location":"architecture/design-docs/#Design-Documents","page":"Design Documents","title":"Design Documents","text":"For in-depth analysis of design decisions, implementation tradeoffs, and architectural choices, see the design documents in the repository:","category":"section"},{"location":"architecture/design-docs/#API-Design","page":"Design Documents","title":"API Design","text":"hybridapidesign.md Two-API strategy (acquire! vs unsafe_acquire!) and type stability analysis","category":"section"},{"location":"architecture/design-docs/#Caching-and-Performance","page":"Design Documents","title":"Caching & Performance","text":"ndarrayapproach_comparison.md N-way cache design (now legacy — replaced by setfield! reuse on Julia 1.11+ CPU), boxing analysis, and ReshapedArray benchmarks\nfixedslotscodegen_design.md Zero-allocation iteration via @generated functions and fixed-slot type dispatch","category":"section"},{"location":"architecture/design-docs/#Macro-Internals","page":"Design Documents","title":"Macro Internals","text":"untrackedacquiredesign.md Macro-based untracked acquire detection and 1-based sentinel pattern","category":"section"},{"location":"architecture/design-docs/#Backend-Extensions","page":"Design Documents","title":"Backend Extensions","text":"cudaextensiondesign.md CUDA backend architecture and package extension loading\n\n","category":"section"},{"location":"architecture/design-docs/#Document-Overview","page":"Design Documents","title":"Document Overview","text":"Document Focus Area Key Insights\nhybridapidesign API strategy View types for zero-alloc, Array for FFI\nndarrayapproach_comparison Caching N-way cache (legacy); setfield! reuse on Julia 1.11+ CPU\nfixedslotscodegen_design Codegen @generated functions enable type-stable iteration\nuntrackedacquiredesign Macro safety Sentinel pattern ensures correct cleanup\ncudaextensiondesign GPU support Seamless CPU/CUDA API parity","category":"section"},{"location":"architecture/design-docs/#See-Also","page":"Design Documents","title":"See Also","text":"How It Works - High-level architecture overview\nType Dispatch & Cache - Technical deep-dive\n@with_pool Macro Internals - Macro transformation details","category":"section"},{"location":"features/configuration/#Configuration","page":"Configuration","title":"Configuration","text":"AdaptiveArrayPools can be configured via LocalPreferences.toml:\n\n[AdaptiveArrayPools]\nuse_pooling = false  # ⭐ Primary: Disable pooling entirely\ncache_ways = 8       # Advanced: N-way cache size (default: 4)","category":"section"},{"location":"features/configuration/#Compile-time:-USE_POOLING-(-Primary)","page":"Configuration","title":"Compile-time: USE_POOLING (⭐ Primary)","text":"The most important configuration. Completely disable pooling to make acquire! behave like standard allocation.\n\n# LocalPreferences.toml\n[AdaptiveArrayPools]\nuse_pooling = false\n\nOr programmatically:\n\nusing Preferences\nPreferences.set_preferences!(AdaptiveArrayPools, \"use_pooling\" => false)\n# Restart Julia for changes to take effect\n\nWhen USE_POOLING = false:\n\npool becomes DisabledPool{backend}() instead of an active pool\nAll pool functions fall back to standard allocation\nBackend context is preserved: :cuda still returns CuArray\n\n# These become equivalent:\n@with_pool pool acquire!(pool, Float64, n, n)  →  Matrix{Float64}(undef, n, n)\n@with_pool pool acquire!(pool, Float64, n)     →  Vector{Float64}(undef, n)\n\n# With CUDA backend:\n@with_pool :cuda pool zeros!(pool, 100)        →  CUDA.zeros(Float32, 100)\n\nUse pooling_enabled(pool) to check if pooling is active.\n\nUse cases:\n\nDebugging: Compare behavior with/without pooling\nBenchmarking: Measure pooling overhead vs direct allocation\nGradual adoption: Add @with_pool annotations now, enable pooling later\nCI/Testing: Run tests without pooling to isolate issues\n\nAll pooling code is completely eliminated at compile time (zero overhead).","category":"section"},{"location":"features/configuration/#Runtime:-MAYBE*POOLING*ENABLED","page":"Configuration","title":"Runtime: MAYBEPOOLINGENABLED","text":"Only affects @maybe_with_pool. Toggle without restart.\n\nMAYBE_POOLING_ENABLED[] = false  # Disable\nMAYBE_POOLING_ENABLED[] = true   # Enable (default)","category":"section"},{"location":"features/configuration/#Runtime:-POOL_DEBUG","page":"Configuration","title":"Runtime: POOL_DEBUG","text":"Enable safety validation to catch direct returns of pool-backed arrays.\n\nPOOL_DEBUG[] = true   # Enable safety checks (development)\nPOOL_DEBUG[] = false  # Disable (default, production)\n\nWhen enabled, returning a pool-backed array from a @with_pool block will throw an error.","category":"section"},{"location":"features/configuration/#Compile-time:-CACHE_WAYS-(Julia-1.10-/-CUDA-only)","page":"Configuration","title":"Compile-time: CACHE_WAYS (Julia 1.10 / CUDA only)","text":"Configure the N-way cache size for unsafe_acquire!. On Julia 1.11+ CPU, this setting has no effect — the setfield!-based wrapper reuse supports unlimited dimension patterns with zero allocation.\n\nThis setting is relevant for:\n\nJulia 1.10 (legacy N-way cache path)\nCUDA backend (N-way cache for CuArray wrappers)\n\n# LocalPreferences.toml\n[AdaptiveArrayPools]\ncache_ways = 8  # Default: 4, Range: 1-16\n\nOr programmatically:\n\nusing AdaptiveArrayPools\nset_cache_ways!(8)\n# Restart Julia for changes to take effect\n\nWhen to increase: If your CUDA code or Julia 1.10 code alternates between more than 4 dimension patterns per pool slot, increase cache_ways to avoid cache eviction (~100 bytes header per miss).","category":"section"},{"location":"features/configuration/#Summary","page":"Configuration","title":"Summary","text":"Setting Scope Restart? Priority Affects\nuse_pooling Compile-time Yes ⭐ Primary All macros, acquire! behavior\ncache_ways Compile-time Yes Advanced unsafe_acquire! N-D caching (Julia 1.10 / CUDA only)\nMAYBE_POOLING_ENABLED Runtime No Optional @maybe_with_pool only\nPOOL_DEBUG Runtime No Debug Safety validation","category":"section"},{"location":"features/maybe-with-pool/#@maybe_with_pool","page":"@maybe_with_pool","title":"@maybe_with_pool","text":"Runtime-toggleable pooling. Users can enable/disable via MAYBE_POOLING_ENABLED[].","category":"section"},{"location":"features/maybe-with-pool/#Usage","page":"@maybe_with_pool","title":"Usage","text":"@maybe_with_pool pool function compute(n)\n    v = acquire!(pool, Float64, n)\n    v .= 1.0\n    sum(v)\nend\n\n# Toggle at runtime\nMAYBE_POOLING_ENABLED[] = false  # Normal allocation\nMAYBE_POOLING_ENABLED[] = true   # Uses pool","category":"section"},{"location":"features/maybe-with-pool/#When-to-Use","page":"@maybe_with_pool","title":"When to Use","text":"Library code where end-users should control pooling behavior\nDebugging: disable pooling to isolate memory issues\nBenchmarking: compare pooled vs non-pooled performance","category":"section"},{"location":"features/maybe-with-pool/#How-It-Works","page":"@maybe_with_pool","title":"How It Works","text":"When MAYBE_POOLING_ENABLED[] == false:\n\npool becomes DisabledPool{backend}() (e.g., DisabledPool{:cpu}() or DisabledPool{:cuda}())\nAll pool functions (acquire!, zeros!, etc.) fall back to standard allocation\nBackend context is preserved: :cuda → CuArray, :cpu → Array\n\nUse pooling_enabled(pool) to check if pooling is active:\n\n@maybe_with_pool pool begin\n    if pooling_enabled(pool)\n        # Using pooled memory\n    else\n        # Using standard allocation (DisabledPool)\n    end\nend","category":"section"},{"location":"features/maybe-with-pool/#vs-@with_pool","page":"@maybe_with_pool","title":"vs @with_pool","text":" @with_pool @maybe_with_pool\nRuntime toggle No Yes\nOverhead when disabled None Branch check\nUse case Application code Library code","category":"section"},{"location":"features/maybe-with-pool/#Safety","page":"@maybe_with_pool","title":"Safety","text":"Same rules as @with_pool: arrays are only valid within the scope. Do not return or store them externally.","category":"section"},{"location":"architecture/type-dispatch/#Type-Dispatch-and-Caching","page":"Type Dispatch & Cache","title":"Type Dispatch & Caching","text":"This page explains the internal mechanisms that enable zero-allocation performance.","category":"section"},{"location":"architecture/type-dispatch/#Fixed-Slot-Type-Dispatch","page":"Type Dispatch & Cache","title":"Fixed-Slot Type Dispatch","text":"To achieve zero-lookup overhead, common types have dedicated struct fields:\n\nstruct AdaptiveArrayPool\n    float64::TypedPool{Float64}\n    float32::TypedPool{Float32}\n    int64::TypedPool{Int64}\n    int32::TypedPool{Int32}\n    complexf64::TypedPool{ComplexF64}\n    complexf32::TypedPool{ComplexF32}\n    bool::TypedPool{Bool}\n    others::IdDict{DataType, Any}  # Fallback for rare types\nend\n\nWhen you call acquire!(pool, Float64, n), the compiler inlines directly to pool.float64 - no dictionary lookup, no type instability.","category":"section"},{"location":"architecture/type-dispatch/#N-D-Wrapper-Caching-for-unsafe_acquire!","page":"Type Dispatch & Cache","title":"N-D Wrapper Caching for unsafe_acquire!","text":"unsafe_acquire! returns native Array types. The caching strategy depends on Julia version:","category":"section"},{"location":"architecture/type-dispatch/#Julia-1.11:-setfield!-based-Wrapper-Reuse","page":"Type Dispatch & Cache","title":"Julia 1.11+: setfield!-based Wrapper Reuse","text":"Julia 1.11 made Array a mutable struct, enabling in-place field mutation:\n\n# Cached wrapper reuse via setfield! (0-alloc)\nsetfield!(cached_arr, :ref, new_memory_ref)   # update backing memory\nsetfield!(cached_arr, :size, new_dims)         # update dimensions\n\nWrappers are stored in nd_wrappers::Vector{Union{Nothing, Vector{Any}}}, indexed directly by dimensionality N (~1ns lookup). Unlimited dimension patterns per slot, zero allocation after warmup.","category":"section"},{"location":"architecture/type-dispatch/#Julia-1.10-/-CUDA:-N-Way-Set-Associative-Cache","page":"Type Dispatch & Cache","title":"Julia 1.10 / CUDA: N-Way Set Associative Cache","text":"On Julia 1.10 (CPU) and CUDA, Array/CuArray fields cannot be mutated. These paths use a 4-way set-associative cache with round-robin eviction (CACHE_WAYS = 4 default):\n\nCache hit (≤4 dim patterns per slot): 0 bytes\nCache miss (>4 patterns): ~80-144 bytes for Array header allocation\n\nSee Configuration for CACHE_WAYS tuning.\n\n","category":"section"},{"location":"architecture/type-dispatch/#View-vs-Array:-When-to-Use-What?","page":"Type Dispatch & Cache","title":"View vs Array: When to Use What?","text":"API Return Type Allocation (Julia 1.11+) Allocation (1.10 / CUDA) Recommended For\nacquire! SubArray / ReshapedArray Always 0 bytes Always 0 bytes 99% of cases\nunsafe_acquire! Vector / Array 0 bytes (setfield! reuse) 0-144 bytes (N-way cache) FFI, type constraints","category":"section"},{"location":"architecture/type-dispatch/#Why-View-is-the-Default","page":"Type Dispatch & Cache","title":"Why View is the Default","text":"Zero-allocation guarantee: Compiler eliminates view wrappers via SROA (Scalar Replacement of Aggregates)\nBLAS/LAPACK compatible: Processed as StridedArray, no performance difference\nType stable: Always returns the same wrapper types\n\nnote: `Bit` masks\nFor T === Bit, both APIs return native BitVector/BitArray{N} (not views) to preserve BitArray-specialized kernels (count, any, all, bitwise ops). Cache hit achieves 0 bytes allocation. These are not StridedArray.","category":"section"},{"location":"architecture/type-dispatch/#When-to-Use-unsafe_acquire!","page":"Type Dispatch & Cache","title":"When to Use unsafe_acquire!","text":"C FFI: When ccall requires Ptr{T} from contiguous memory\n\narr = unsafe_acquire!(pool, Float64, 100)\nccall(:c_function, Cvoid, (Ptr{Float64}, Cint), arr, 100)\n\nType signature constraints: Function explicitly requires Array{T,N}\n\nfunction process(data::Array{Float64,2})\n    # Only accepts Array, not AbstractArray\nend\n\nm = unsafe_acquire!(pool, Float64, 10, 10)\nprocess(m)  # Works\n\nRuntime dispatch avoidance: When types are determined at runtime\n\n# Polymorphic code where type stability matters\nfunction dispatch_heavy(pool, T)\n    arr = unsafe_acquire!(pool, T, 100)  # Concrete Array type\n    # ... operations that would trigger dispatch with views\nend","category":"section"},{"location":"architecture/type-dispatch/#Performance-Comparison","page":"Type Dispatch & Cache","title":"Performance Comparison","text":"Operation acquire! (View) unsafe_acquire! (Array)\nAllocation (Julia 1.11+) 0 bytes 0 bytes (setfield! reuse)\nAllocation (Julia 1.10 / CUDA) 0 bytes 0 bytes (hit) / 80-144 bytes (miss)\nBLAS operations Identical Identical\nType stability Guaranteed Guaranteed\nFFI compatibility Requires conversion Direct","category":"section"},{"location":"architecture/type-dispatch/#Header-Size-by-Dimensionality-(Julia-1.10-/-CUDA-only)","page":"Type Dispatch & Cache","title":"Header Size by Dimensionality (Julia 1.10 / CUDA only)","text":"On Julia 1.11+ CPU, unsafe_acquire! is always zero-allocation via setfield! reuse. On Julia 1.10 and CUDA, a cache miss allocates an Array header:\n\nDimensions Header Size\n1D (Vector) 80 bytes\n2D-3D 112 bytes\n4D-5D 144 bytes\n\nThis is Julia's internal Array metadata; actual data memory is always reused from the pool.\n\n","category":"section"},{"location":"architecture/type-dispatch/#See-Also","page":"Type Dispatch & Cache","title":"See Also","text":"How It Works - Checkpoint/Rewind mechanism\nDesign Documents - Detailed design analysis\nConfiguration - Cache tuning options","category":"section"},{"location":"architecture/macro-internals/#How-@with_pool-Works","page":"@with_pool Internals","title":"How @with_pool Works","text":"This page explains the internal mechanics of the @with_pool macro for advanced users and contributors who want to understand the optimization strategies.","category":"section"},{"location":"architecture/macro-internals/#Overview","page":"@with_pool Internals","title":"Overview","text":"The @with_pool macro provides automatic lifecycle management with three key optimizations:\n\nTry-Finally Safety — Guarantees cleanup even on exceptions\nTyped Checkpoint/Rewind — Only saves/restores used types (~77% faster)\nUntracked Acquire Detection — Safely handles acquire! calls outside macro visibility","category":"section"},{"location":"architecture/macro-internals/#Basic-Lifecycle-Flow","page":"@with_pool Internals","title":"Basic Lifecycle Flow","text":"┌─────────────────────────────────────────────────────────────┐\n│  @with_pool pool function foo(x)                            │\n│      A = acquire!(pool, Float64, 100)                       │\n│      B = similar!(pool, A)                                  │\n│      return sum(A) + sum(B)                                 │\n│  end                                                        │\n└─────────────────────────────────────────────────────────────┘\n                              ↓\n              ┌───────────────────────────────────┐\n              │       Macro Transformation        │\n              └───────────────────────────────────┘\n                              ↓\n┌─────────────────────────────────────────────────────────────┐\n│  function foo(x)                                            │\n│      pool = get_task_local_pool()                           │\n│      checkpoint!(pool, Float64)     # ← Type-specific       │\n│      try                                                    │\n│          A = _acquire_impl!(pool, Float64, 100)             │\n│          B = _similar_impl!(pool, A)                        │\n│          return sum(A) + sum(B)                             │\n│      finally                                                │\n│          rewind!(pool, Float64)     # ← Type-specific       │\n│      end                                                    │\n│  end                                                        │\n└─────────────────────────────────────────────────────────────┘","category":"section"},{"location":"architecture/macro-internals/#Key-Points","page":"@with_pool Internals","title":"Key Points","text":"try-finally ensures rewind! executes even if an exception occurs\nacquire! → _acquire_impl! transformation bypasses untracked marking overhead\nType-specific checkpoint!(pool, Float64) is ~77% faster than full checkpoint","category":"section"},{"location":"architecture/macro-internals/#Type-Extraction:-Static-Analysis-at-Compile-Time","page":"@with_pool Internals","title":"Type Extraction: Static Analysis at Compile Time","text":"The macro analyzes the AST to extract types used in acquire! calls:\n\n# Macro sees these acquire! calls:\n@with_pool pool begin\n    A = acquire!(pool, Float64, 10, 10)     # → extracts Float64\n    B = zeros!(pool, ComplexF64, 100)       # → extracts ComplexF64\n    C = similar!(pool, A)                    # → extracts eltype(A) → Float64\nend\n\n# Generated code uses typed checkpoint/rewind:\ncheckpoint!(pool, Float64, ComplexF64)\ntry\n    ...\nfinally\n    rewind!(pool, Float64, ComplexF64)\nend","category":"section"},{"location":"architecture/macro-internals/#Type-Extraction-Rules","page":"@with_pool Internals","title":"Type Extraction Rules","text":"Call Pattern Extracted Type\nacquire!(pool, Float64, dims...) Float64\nacquire!(pool, x) eltype(x) (if x is external)\nzeros!(pool, dims...) default_eltype(pool)\nzeros!(pool, Float32, dims...) Float32\nsimilar!(pool, x) eltype(x)\nsimilar!(pool, x, Int64, ...) Int64","category":"section"},{"location":"architecture/macro-internals/#When-Type-Extraction-Fails-Full-Checkpoint","page":"@with_pool Internals","title":"When Type Extraction Fails → Full Checkpoint","text":"The macro falls back to full checkpoint!(pool) when:\n\n@with_pool pool begin\n    T = eltype(data)                  # T defined locally AFTER checkpoint\n    A = acquire!(pool, T, 100)        # Can't use T at checkpoint time!\nend\n# → Falls back to checkpoint!(pool) / rewind!(pool)\n\n@with_pool pool begin\n    local_arr = compute()             # local_arr defined AFTER checkpoint\n    B = similar!(pool, local_arr)     # eltype(local_arr) unavailable\nend\n# → Falls back to checkpoint!(pool) / rewind!(pool)","category":"section"},{"location":"architecture/macro-internals/#Untracked-Acquire-Detection","page":"@with_pool Internals","title":"Untracked Acquire Detection","text":"","category":"section"},{"location":"architecture/macro-internals/#The-Problem","page":"@with_pool Internals","title":"The Problem","text":"The macro can only see acquire! calls directly in its AST. Calls inside helper functions are invisible:\n\nfunction helper!(pool)\n    return zeros!(pool, Float64, 100)   # Macro can't see this!\nend\n\n@with_pool pool begin\n    A = acquire!(pool, Int64, 10)       # ← Macro sees this (Int64)\n    B = helper!(pool)                    # ← Macro can't see Float64 inside!\nend\n\n# If only checkpoint!(pool, Int64), Float64 arrays won't be rewound!","category":"section"},{"location":"architecture/macro-internals/#The-Solution:-Bitmask-Based-Type-Touch-Tracking","page":"@with_pool Internals","title":"The Solution: Bitmask-Based Type Touch Tracking","text":"Every acquire! call (and convenience functions) records the type touch with type-specific bitmask information:\n\n# Public API (called from user code outside macro)\n@inline function acquire!(pool, ::Type{T}, n::Int) where {T}\n    _record_type_touch!(pool, T)         # ← Records type-specific bitmask!\n    _acquire_impl!(pool, T, n)\nend\n\n# Macro-transformed calls skip the recording\n# (because macro already knows about them)\n_acquire_impl!(pool, T, n)               # ← No recording\n\nEach fixed-slot type maps to a bit in a UInt16 bitmask via _fixed_slot_bit(T). Non-fixed-slot types set a separate _touched_has_others flag.","category":"section"},{"location":"architecture/macro-internals/#Flow-Diagram","page":"@with_pool Internals","title":"Flow Diagram","text":"@with_pool pool begin                    Bitmask state at depth 2\n    │                                    ─────────────────────────────\n    ├─► checkpoint!(pool, Int64)         masks[2]=0x0000, others[2]=false\n    │\n    │   A = _acquire_impl!(...)          (macro-transformed, no mark)\n    │   B = helper!(pool)\n    │       └─► zeros!(pool, Float64, N)\n    │           └─► _record_type_touch!(pool, Float64)\n    │               masks[2] |= 0x0001 (Float64 bit) ←───┐\n    │                                                      │\n    │   ... more code ...                                  │\n    │                                                      │\n    └─► rewind! check:                                     │\n        tracked_mask = _tracked_mask_for_types(Int64)      │\n        if _can_use_typed_path(pool, tracked_mask) ────────┘\n            rewind!(pool, Int64)     # Typed rewind (fast)\n        else                         # Float64 not in {Int64} → full\n            rewind!(pool)            # Full rewind (safe)\n        end\nend","category":"section"},{"location":"architecture/macro-internals/#Why-This-Works","page":"@with_pool Internals","title":"Why This Works","text":"Macro-tracked calls: Transformed to _acquire_impl! → no bitmask touch → typed path\nExternal calls: Use public API → records type-specific bitmask → subset check at rewind\nSubset optimization: If touched types are a subset of tracked types, the typed path is still safe\nResult: Always safe, with finer-grained optimization than a single boolean flag","category":"section"},{"location":"architecture/macro-internals/#Nested-@with_pool-Handling","page":"@with_pool Internals","title":"Nested @with_pool Handling","text":"Each @with_pool maintains its own checkpoint depth:\n\n@with_pool p1 begin                      depth: 1 → 2\n    v1 = acquire!(p1, Float64, 10)\n    │\n    ├─► @with_pool p2 begin              depth: 2 → 3\n    │       v2 = acquire!(p2, Int64, 5)\n    │       helper!(p2)                  # marks bitmask at depth 3\n    │       sum(v2)\n    │   end                              depth: 3 → 2, bitmask checked\n    │\n    │   # v1 still valid here!\n    sum(v1)\nend                                      depth: 2 → 1, bitmask checked","category":"section"},{"location":"architecture/macro-internals/#Depth-Tracking-Data-Structures","page":"@with_pool Internals","title":"Depth Tracking Data Structures","text":"struct AdaptiveArrayPool\n    # ... type pools ...\n    _current_depth::Int                     # Current scope depth (1 = global)\n    _touched_type_masks::Vector{UInt16}     # Per-depth: which fixed slots were touched\n    _touched_has_others::Vector{Bool}       # Per-depth: any non-fixed-slot type touched\nend\n\n# Initialized with sentinel:\n_current_depth = 1                          # Global scope\n_touched_type_masks = [UInt16(0)]           # Sentinel for depth=1\n_touched_has_others = [false]               # Sentinel for depth=1","category":"section"},{"location":"architecture/macro-internals/#Performance-Impact","page":"@with_pool Internals","title":"Performance Impact","text":"Scenario Checkpoint Method Relative Speed\n1 type, no untracked checkpoint!(pool, T) ~77% faster\nMultiple types, no untracked checkpoint!(pool, T1, T2, ...) ~50% faster\nUntracked subset of tracked checkpoint!(pool, T...) ~77% faster\nUnknown untracked types checkpoint!(pool) Baseline\n\nThe optimization matters most in tight loops with many iterations. The bitmask subset check allows the typed path even when untracked acquires occur, as long as those types are already covered by the macro's tracked set.","category":"section"},{"location":"architecture/macro-internals/#Code-Generation-Summary","page":"@with_pool Internals","title":"Code Generation Summary","text":"# INPUT\n@with_pool pool function compute(data)\n    A = acquire!(pool, Float64, length(data))\n    result = helper!(pool, A)  # May have untracked acquires\n    return result\nend\n\n# OUTPUT (simplified)\nfunction compute(data)\n    pool = get_task_local_pool()\n\n    # Bitmask subset check: can typed path handle any untracked acquires?\n    if _can_use_typed_path(pool, _tracked_mask_for_types(Float64))\n        checkpoint!(pool, Float64)           # Typed checkpoint (fast)\n    else\n        checkpoint!(pool)                    # Full checkpoint (safe)\n    end\n\n    try\n        A = _acquire_impl!(pool, Float64, length(data))\n        result = helper!(pool, A)\n        return result\n    finally\n        if _can_use_typed_path(pool, _tracked_mask_for_types(Float64))\n            rewind!(pool, Float64)           # Typed rewind (fast)\n        else\n            rewind!(pool)                    # Full rewind (safe)\n        end\n    end\nend","category":"section"},{"location":"architecture/macro-internals/#Key-Internal-Functions","page":"@with_pool Internals","title":"Key Internal Functions","text":"Function Purpose\n_extract_acquire_types(expr, pool_name) AST walk to find types\n_filter_static_types(types, local_vars) Filter out locally-defined types\n_transform_acquire_calls(expr, pool_name) Replace acquire! → _acquire_impl!\n_record_type_touch!(pool, T) Record type touch in bitmask for current depth\n_can_use_typed_path(pool, mask) Bitmask subset check for typed vs full path\n_tracked_mask_for_types(T...) Compile-time bitmask for tracked types\n_generate_typed_checkpoint_call(pool, types) Generate bitmask-aware checkpoint","category":"section"},{"location":"architecture/macro-internals/#See-Also","page":"@with_pool Internals","title":"See Also","text":"How It Works — Overview of pool architecture\nSafety Rules — Scope rules and best practices\nConfiguration — Performance tuning options","category":"section"},{"location":"basics/quick-start/#Quick-Start","page":"Quick Start","title":"Quick Start","text":"This guide will help you get up and running with AdaptiveArrayPools.jl in minutes.","category":"section"},{"location":"basics/quick-start/#Installation","page":"Quick Start","title":"Installation","text":"using Pkg\nPkg.Registry.add(Pkg.RegistrySpec(url=\"https://github.com/ProjectTorreyPines/FuseRegistry.jl.git\"))\nPkg.add(\"AdaptiveArrayPools\")","category":"section"},{"location":"basics/quick-start/#Basic-Usage","page":"Quick Start","title":"Basic Usage","text":"The core workflow is simple:\n\nWrap your function with @with_pool\nReplace allocations with acquire! or convenience functions\nReturn computed values (scalars, copies), not the arrays themselves","category":"section"},{"location":"basics/quick-start/#Before-(Standard-Julia)","page":"Quick Start","title":"Before (Standard Julia)","text":"function compute(n)\n    A = rand(n, n)      # allocates\n    B = rand(n, n)      # allocates\n    C = A * B           # allocates\n    return sum(C)\nend\n\nfor i in 1:10_000\n    compute(100)  # 90k allocations, 2.75 GiB, 31% GC time\nend","category":"section"},{"location":"basics/quick-start/#After-(With-Pooling)","page":"Quick Start","title":"After (With Pooling)","text":"using AdaptiveArrayPools, LinearAlgebra, Random\n\n@with_pool pool function compute_pooled(n)\n    A = acquire!(pool, Float64, n, n)  # reuses memory\n    B = similar!(pool, A)\n    C = similar!(pool, A)\n\n    rand!(A); rand!(B)\n    mul!(C, A, B)\n    return sum(C)\nend\n\ncompute_pooled(100)  # warmup (first call allocates)\nfor i in 1:10_000\n    compute_pooled(100)  # zero allocations, 0% GC\nend","category":"section"},{"location":"basics/quick-start/#Convenience-Functions","page":"Quick Start","title":"Convenience Functions","text":"Common initialization patterns have shortcuts:\n\nFunction Equivalent to\nzeros!(pool, 10) acquire! + fill!(0)\nones!(pool, Float32, 3, 3) acquire! + fill!(1)\nsimilar!(pool, A) acquire! matching eltype(A), size(A)\nreshape!(pool, A, 3, 4) Reshape sharing memory, zero-alloc (1.11+)\n\n@with_pool pool function example(n)\n    A = zeros!(pool, n, n)        # zero-initialized\n    B = ones!(pool, Float32, n)   # Float32 ones\n    C = similar!(pool, A)         # same type and size as A\n    # ...\nend","category":"section"},{"location":"basics/quick-start/#Return-Types","page":"Quick Start","title":"Return Types","text":"acquire! and convenience functions usually return view types (SubArray, ReshapedArray) that work seamlessly with BLAS/LAPACK. For Bit masks, acquire! returns native BitVector/BitArray{N} (bit-packed):\n\nA = acquire!(pool, Float64, 10, 10)  # ReshapedArray{Float64,2}\nmul!(C, A, B)  # works perfectly with BLAS\n\nmask = acquire!(pool, Bit, 1000)     # BitVector (bit-packed)\n\nIf you need native Array types (FFI, type constraints), use unsafe_acquire!:\n\nA = unsafe_acquire!(pool, Float64, 10, 10)  # Array{Float64,2}","category":"section"},{"location":"basics/quick-start/#Important-Safety-Rules","page":"Quick Start","title":"Important Safety Rules","text":"Arrays from the pool are only valid within the @with_pool scope:\n\n# DO NOT return pool-backed arrays\n@with_pool pool function bad_example()\n    A = acquire!(pool, Float64, 10)\n    return A  # WRONG - A marked for reuse, data may be overwritten!\nend\n\n# Return computed values instead\n@with_pool pool function good_example()\n    A = acquire!(pool, Float64, 10)\n    return sum(A)  # OK - returning a scalar\nend\n\nFor complete safety guidelines, see Safety Rules.","category":"section"},{"location":"basics/quick-start/#Next-Steps","page":"Quick Start","title":"Next Steps","text":"Safety Rules - Complete scope rules and anti-patterns\nFull API Reference - Complete function and macro reference\nConfiguration - Preferences and cache tuning\nMulti-threading - Task/thread safety patterns\nCUDA Support - GPU backend usage","category":"section"},{"location":"features/cuda-support/#CUDA-Backend","page":"CUDA Support","title":"CUDA Backend","text":"AdaptiveArrayPools provides native CUDA support through a package extension that loads automatically when CUDA.jl is available.","category":"section"},{"location":"features/cuda-support/#Quick-Start","page":"CUDA Support","title":"Quick Start","text":"using AdaptiveArrayPools, CUDA\n\n# Use :cuda backend for GPU arrays\n@with_pool :cuda pool function gpu_computation(n)\n    A = acquire!(pool, Float64, n, n)  # CuArray view\n    B = acquire!(pool, Float64, n, n)  # CuArray view\n\n    fill!(A, 1.0)\n    fill!(B, 2.0)\n\n    return sum(A .+ B)\nend\n\n# Zero GPU allocation in hot loops\nfor i in 1:1000\n    gpu_computation(100)  # GPU memory reused from pool\nend","category":"section"},{"location":"features/cuda-support/#API","page":"CUDA Support","title":"API","text":"The CUDA backend uses the same API as CPU, with :cuda backend specifier:\n\nMacro/Function Description\n@with_pool :cuda pool expr GPU pool with automatic checkpoint/rewind\nacquire!(pool, T, dims...) Returns CuArray view (always 0 bytes GPU alloc)\nunsafe_acquire!(pool, T, dims...) Returns raw CuArray (for FFI/type constraints)\nget_task_local_cuda_pool() Returns the task-local CUDA pool\npool_stats(:cuda) Print CUDA pool statistics","category":"section"},{"location":"features/cuda-support/#Return-Types","page":"CUDA Support","title":"Return Types","text":"Function 1D Return N-D Return\nacquire! CuArray{T,1} (view) CuArray{T,N} (view)\nunsafe_acquire! CuArray{T,1} CuArray{T,N}","category":"section"},{"location":"features/cuda-support/#Allocation-Behavior","page":"CUDA Support","title":"Allocation Behavior","text":"GPU Memory: Always 0 bytes allocation after warmup. The underlying CuVector is resized as needed and reused.\n\nCPU-side Wrapper Memory (for unsafe_acquire! N-D on CUDA):\n\nThe CUDA backend uses an N-way set-associative cache for CuArray wrapper reuse\nCache hit (≤CACHE_WAYS dimension patterns per slot): 0 bytes\nCache miss (>CACHE_WAYS patterns): ~100 bytes for wrapper metadata\nSee Configuration for CACHE_WAYS tuning\n\nnote: CPU vs CUDA caching\nOn CPU (Julia 1.11+), unsafe_acquire! uses setfield!-based wrapper reuse with zero allocation for any number of dimension patterns. The CUDA backend does not yet support this optimization and still uses the N-way cache.\n\n# Example: 4 patterns fit in default 4-way cache → zero CPU-side allocation\ndims_list = ((10, 10), (5, 20), (20, 5), (4, 25))\nfor dims in dims_list\n    @with_pool :cuda p begin\n        A = acquire!(p, Float64, dims...)\n        # Use A...\n    end\nend","category":"section"},{"location":"features/cuda-support/#Fixed-Slot-Types","page":"CUDA Support","title":"Fixed Slot Types","text":"Optimized types with pre-allocated slots (same as CPU):\n\nType Field\nFloat64 .float64\nFloat32 .float32\nFloat16 .float16\nInt64 .int64\nInt32 .int32\nComplexF64 .complexf64\nComplexF32 .complexf32\nBool .bool\n\nOther types use the fallback dictionary (.others).","category":"section"},{"location":"features/cuda-support/#Limitations","page":"CUDA Support","title":"Limitations","text":"No @maybe_with_pool :cuda: Runtime toggle not supported for CUDA backend\nTask-local only: Each Task gets its own CUDA pool, same as CPU\nSame device: All arrays in a pool use the same CUDA device","category":"section"},{"location":"features/cuda-support/#Example:-Matrix-Multiplication","page":"CUDA Support","title":"Example: Matrix Multiplication","text":"using AdaptiveArrayPools, CUDA, LinearAlgebra\n\n@with_pool :cuda pool function gpu_matmul(n)\n    A = acquire!(pool, Float64, n, n)\n    B = acquire!(pool, Float64, n, n)\n    C = acquire!(pool, Float64, n, n)\n\n    rand!(A); rand!(B)\n    mul!(C, A, B)\n\n    return sum(C)\nend\n\n# Warmup\ngpu_matmul(100)\n\n# Benchmark - zero GPU allocation\nusing BenchmarkTools\n@benchmark gpu_matmul(1000)","category":"section"},{"location":"features/cuda-support/#Debugging","page":"CUDA Support","title":"Debugging","text":"# Check pool state\npool_stats(:cuda)\n\n# Output:\n# CuAdaptiveArrayPool (device 0)\n#   Float64 (fixed) [GPU]\n#     slots: 3 (active: 0)\n#     elements: 30000 (234.375 KiB)","category":"section"},{"location":"reference/api/#API-Reference","page":"Full API","title":"API Reference","text":"","category":"section"},{"location":"reference/api/#API-Summary","page":"Full API","title":"API Summary","text":"","category":"section"},{"location":"reference/api/#Macros","page":"Full API","title":"Macros","text":"Macro Description\n@with_pool name expr Recommended. Injects a global, task-local pool named name. Automatically checkpoints and rewinds.\n@maybe_with_pool name expr Same as @with_pool, but can be toggled on/off at runtime via MAYBE_POOLING_ENABLED[].","category":"section"},{"location":"reference/api/#Functions","page":"Full API","title":"Functions","text":"Function Description\nacquire!(pool, T, dims...) Returns a view for most T: SubArray{T,1} for 1D, ReshapedArray{T,N} for N-D. For T === Bit, returns native BitVector/BitArray{N}. Cache hit 0 bytes.\nacquire!(pool, T, dims::Tuple) Tuple overload for acquire! (e.g., acquire!(pool, T, size(x))).\nacquire!(pool, x::AbstractArray) Similar-style: acquires array matching eltype(x) and size(x).\nunsafe_acquire!(pool, T, dims...) Returns native Array/CuArray (CPU: Vector{T} for 1D, Array{T,N} for N-D). For T === Bit, returns native BitVector/BitArray{N} (equivalent to acquire!). Only use for FFI/type constraints.\nunsafe_acquire!(pool, x::AbstractArray) Similar-style: acquires raw array matching eltype(x) and size(x).\ncheckpoint!(pool) Saves the current pool state (stack pointer).\nrewind!(pool) Restores the pool to the last checkpoint, freeing all arrays acquired since then.\npool_stats(pool) Prints detailed statistics about pool usage.\nget_task_local_pool() Returns the task-local pool instance.\nempty!(pool) Clears all internal storage, releasing all memory.","category":"section"},{"location":"reference/api/#Convenience-Functions","page":"Full API","title":"Convenience Functions","text":"Default element type is Float64 (CPU) or Float32 (CUDA).\n\nFunction Description\nzeros!(pool, [T,] dims...) Zero-initialized view. Equivalent to acquire! + fill!(0).\nones!(pool, [T,] dims...) One-initialized view. Equivalent to acquire! + fill!(1).\ntrues!(pool, dims...) Bit-packed BitVector / BitArray{N} filled with true.\nfalses!(pool, dims...) Bit-packed BitVector / BitArray{N} filled with false.\nsimilar!(pool, A) View matching eltype(A) and size(A).\nreshape!(pool, A, dims...) Reshape A to dims, sharing memory. Zero-alloc on Julia 1.11+.","category":"section"},{"location":"reference/api/#Types","page":"Full API","title":"Types","text":"Type Description\nAdaptiveArrayPool The main pool type. Create with AdaptiveArrayPool().\nBit Sentinel type to request packed BitVector storage (1 bit/element).\nDisabledPool{Backend} Sentinel type when pooling is disabled.","category":"section"},{"location":"reference/api/#Configuration-and-Utilities","page":"Full API","title":"Configuration & Utilities","text":"Symbol Description\nUSE_POOLING Compile-time constant to disable all pooling.\nMAYBE_POOLING_ENABLED Runtime Ref{Bool} for @maybe_with_pool.\nPOOL_DEBUG Runtime Ref{Bool} to enable safety validation.\nset_cache_ways!(n) Set N-way cache size (Julia 1.10 / CUDA only; no effect on Julia 1.11+ CPU).\n\n","category":"section"},{"location":"reference/api/#Detailed-Reference","page":"Full API","title":"Detailed Reference","text":"","category":"section"},{"location":"reference/api/#AdaptiveArrayPools.@maybe_with_pool-Tuple{Any, Any}","page":"Full API","title":"AdaptiveArrayPools.@maybe_with_pool","text":"@maybe_with_pool pool_name expr\n@maybe_with_pool expr\n\nConditionally enables pooling based on MAYBE_POOLING_ENABLED[]. If disabled, pool_name becomes nothing, and acquire! falls back to standard allocation.\n\nUseful for libraries that want to let users control pooling behavior at runtime.\n\nFunction Definition\n\nLike @with_pool, wrap function definitions:\n\n@maybe_with_pool pool function process_data(data)\n    tmp = acquire!(pool, Float64, length(data))  # Conditionally pooled\n    tmp .= data\n    sum(tmp)\nend\n\nBlock Usage\n\nMAYBE_POOLING_ENABLED[] = false\n@maybe_with_pool pool begin\n    v = acquire!(pool, Float64, 100)  # Falls back to Vector{Float64}(undef, 100)\nend\n\n\n\n\n\n","category":"macro"},{"location":"reference/api/#AdaptiveArrayPools.@with_pool-Tuple{Any, Any}","page":"Full API","title":"AdaptiveArrayPools.@with_pool","text":"@with_pool pool_name expr\n@with_pool expr\n@with_pool :backend pool_name expr\n@with_pool :backend expr\n\nExecutes code within a pooling scope with automatic lifecycle management. Calls checkpoint! on entry and rewind! on exit (even if errors occur).\n\nIf pool_name is omitted, a hidden variable is used (useful when you don't need to reference the pool directly).\n\nBackend Selection\n\nUse a symbol to specify the pool backend:\n\n:cpu - CPU pools (default)\n:cuda - GPU pools (requires using CUDA)\n\n# CPU (default)\n@with_pool pool begin ... end\n\n# GPU via CUDA\n@with_pool :cuda pool begin ... end\n\nFunction Definition\n\nWrap function definitions to inject pool lifecycle into the body:\n\n# Long form function\n@with_pool pool function compute_stats(data)\n    tmp = acquire!(pool, Float64, length(data))\n    tmp .= data\n    mean(tmp), std(tmp)\nend\n\n# Short form function\n@with_pool pool fast_sum(data) = begin\n    tmp = acquire!(pool, eltype(data), length(data))\n    tmp .= data\n    sum(tmp)\nend\n\nBlock Usage\n\n# With explicit pool name\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)\n    v .= 1.0\n    sum(v)\nend\n\n# Without pool name (for simple blocks)\n@with_pool begin\n    inner_function()  # inner function can use get_task_local_pool()\nend\n\nNesting\n\nNested @with_pool blocks work correctly - each maintains its own checkpoint.\n\n@with_pool p1 begin\n    v1 = acquire!(p1, Float64, 10)\n    inner = @with_pool p2 begin\n        v2 = acquire!(p2, Float64, 5)\n        sum(v2)\n    end\n    # v1 is still valid here\n    sum(v1) + inner\nend\n\n\n\n\n\n","category":"macro"},{"location":"reference/api/#AdaptiveArrayPools._acquire_impl!-Union{Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Int64}} where T","page":"Full API","title":"AdaptiveArrayPools._acquire_impl!","text":"_acquire_impl!(pool, Type{T}, n) -> SubArray{T,1,Vector{T},...}\n_acquire_impl!(pool, Type{T}, dims...) -> ReshapedArray{T,N,...}\n\nInternal implementation of acquire!. Called directly by macro-transformed code (no type touch recording). User code calls acquire! which adds recording.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._can_use_typed_path-Tuple{AbstractArrayPool, UInt16}","page":"Full API","title":"AdaptiveArrayPools._can_use_typed_path","text":"_can_use_typed_path(pool::AbstractArrayPool, tracked_mask::UInt16) -> Bool\n\nCheck if the typed (fast) checkpoint/rewind path is safe to use.\n\nReturns true when all touched types at the current depth are a subset of the tracked types (bitmask subset check) AND no non-fixed-slot types were touched.\n\nThe subset check: (touched_mask & ~tracked_mask) == 0 means every bit set in touched_mask is also set in tracked_mask.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._claim_slot!-Union{Tuple{AdaptiveArrayPools.TypedPool{T}}, Tuple{T}} where T","page":"Full API","title":"AdaptiveArrayPools._claim_slot!","text":"_claim_slot!(tp::TypedPool{T}) -> Int\n\nClaim the next slot index by incrementing n_active. Ensures the slot exists in vectors/views/viewlengths arrays. The backing vector at this slot is unused — this is for wrapper-only caching (e.g., reshape! uses the slot index for `ndwrapper` storage only).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._disabled_pool_expr-Tuple{Symbol}","page":"Full API","title":"AdaptiveArrayPools._disabled_pool_expr","text":"_disabled_pool_expr(backend::Symbol) -> Expr\n\nGenerate expression for DisabledPool singleton based on backend. Used when pooling is disabled to preserve backend context.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._ensure_body_has_toplevel_lnn-Tuple{Any, Union{Nothing, LineNumberNode}}","page":"Full API","title":"AdaptiveArrayPools._ensure_body_has_toplevel_lnn","text":"_ensure_body_has_toplevel_lnn(body, source)\n\nEnsure body has a LineNumberNode pointing to user source at the top level.\n\nScans first few args to handle Expr(:meta, ...) from @inline etc.\nIf first LNN points to user file (same as source.file), preserve it\nIf first LNN points elsewhere (e.g., macros.jl), replace with source LNN\nIf no LNN exists, prepend source LNN\nIf source.file === :none (REPL/eval), don't clobber valid file LNNs\n\nReturns a new Expr to avoid mutating the original AST.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._extract_acquire_types","page":"Full API","title":"AdaptiveArrayPools._extract_acquire_types","text":"_extract_acquire_types(expr, target_pool) -> Set{Any}\n\nExtract type arguments from acquire/convenience function calls in an expression. Only extracts types from calls where the first argument matches target_pool. This prevents AST pollution when multiple pools are used in the same block.\n\nSupported functions:\n\nacquire! and its alias acquire_view!\nunsafe_acquire! and its alias acquire_array!\nzeros!, ones!, similar!\nunsafe_zeros!, unsafe_ones!, unsafe_similar!\n\nHandles various forms:\n\n[unsafe_]acquire!(pool, Type, dims...): extracts Type directly\nacquire!(pool, x): generates eltype(x) expression\nzeros!(pool, dims...) / ones!(pool, dims...): Float64 (default)\nzeros!(pool, Type, dims...) / ones!(pool, Type, dims...): extracts Type\nsimilar!(pool, x): generates eltype(x) expression\nsimilar!(pool, x, Type, ...): extracts Type\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools._extract_local_assignments","page":"Full API","title":"AdaptiveArrayPools._extract_local_assignments","text":"_extract_local_assignments(expr, locals=Set{Symbol}()) -> Set{Symbol}\n\nFind all symbols that are assigned locally in the expression body. These cannot be used for typed checkpoint since they're defined after checkpoint!.\n\nDetects patterns like: T = eltype(x), local T = ..., etc.\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools._filter_static_types","page":"Full API","title":"AdaptiveArrayPools._filter_static_types","text":"_filter_static_types(types, local_vars=Set{Symbol}()) -> (static_types, has_dynamic)\n\nFilter types for typed checkpoint/rewind generation.\n\nSymbols NOT in local_vars are passed through (type parameters, global types)\nSymbols IN local_vars trigger fallback (defined after checkpoint!)\nParametric types like Vector{T} trigger fallback\neltype(x) expressions: usable if x does NOT reference a local variable\n\nType parameters (T, S from where clause) resolve to concrete types at runtime. Local variables (T = eltype(x)) are defined after checkpoint! and cannot be used.\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools._find_first_lnn_index-Tuple{Any}","page":"Full API","title":"AdaptiveArrayPools._find_first_lnn_index","text":"_find_first_lnn_index(args) -> Union{Int, Nothing}\n\nFind the index of the first LineNumberNode in the leading prefix of args.\n\nScans sequentially, skipping Expr(:meta, ...) nodes (inserted by @inline, @inbounds, etc.). Returns nothing as soon as a non-meta, non-LNN expression is encountered—this prevents matching LNNs deeper in the AST.\n\nExample AST prefix patterns\n\n[Expr(:meta,:inline), LNN, ...] → returns 2\n[LNN, ...] → returns 1\n[Expr(:meta,:inline), Expr(:call,...), LNN, ...] → returns nothing (stopped at call)\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._fix_try_body_lnn!-Tuple{Any, Union{Nothing, LineNumberNode}}","page":"Full API","title":"AdaptiveArrayPools._fix_try_body_lnn!","text":"_fix_try_body_lnn!(expr, source)\n\nFix LineNumberNodes inside try blocks to point to user source. Julia's stack trace uses the LAST LNN before error location for line numbers. By replacing the first LNN in try body with source LNN, we ensure correct line numbers in stack traces.\n\nScans first few args to handle Expr(:meta, ...) from @inline etc. If source.file === :none (REPL/eval), don't clobber valid file LNNs. Modifies expr in-place and returns it.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._generate_function_pool_code_with_backend-Tuple{Symbol, Any, Any, Bool}","page":"Full API","title":"AdaptiveArrayPools._generate_function_pool_code_with_backend","text":"_generate_function_pool_code_with_backend(backend, pool_name, func_def, disable_pooling)\n\nGenerate function code for a specific backend (e.g., :cuda). Wraps the function body with pool getter, checkpoint, try-finally, rewind.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._generate_lazy_checkpoint_call-Tuple{Any}","page":"Full API","title":"AdaptiveArrayPools._generate_lazy_checkpoint_call","text":"_generate_lazy_checkpoint_call(pool_expr)\n\nGenerate a depth-only checkpoint call for dynamic-selective mode (use_typed=false). Much lighter than full checkpoint!: only increments depth and pushes bitmask sentinels.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._generate_lazy_rewind_call-Tuple{Any}","page":"Full API","title":"AdaptiveArrayPools._generate_lazy_rewind_call","text":"_generate_lazy_rewind_call(pool_expr)\n\nGenerate selective rewind code for dynamic-selective mode (use_typed=false). Delegates to _lazy_rewind! — a single function call, symmetric with _lazy_checkpoint! for checkpoint. This avoids let-block overhead in finally clauses (which can impair Julia's type inference and cause boxing).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._generate_pool_code_with_backend-Tuple{Symbol, Any, Any, Bool}","page":"Full API","title":"AdaptiveArrayPools._generate_pool_code_with_backend","text":"_generate_pool_code_with_backend(backend, pool_name, expr, force_enable)\n\nGenerate pool code for a specific backend (e.g., :cuda, :cpu). Uses _get_pool_for_backend(Val{backend}()) for zero-overhead dispatch.\n\nIncludes type-specific checkpoint/rewind optimization (same as regular @with_pool).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._generate_typed_checkpoint_call-Tuple{Any, Any}","page":"Full API","title":"AdaptiveArrayPools._generate_typed_checkpoint_call","text":"_generate_typed_checkpoint_call(pool_expr, types)\n\nGenerate bitmask-aware checkpoint call. When types are known at compile time, emits a conditional:\n\nif touched types ⊆ tracked types → typed checkpoint (fast path)\notherwise → _typed_lazy_checkpoint! (typed checkpoint + set bit 14 for lazy first-touch checkpointing of extra types touched by helpers)\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._generate_typed_rewind_call-Tuple{Any, Any}","page":"Full API","title":"AdaptiveArrayPools._generate_typed_rewind_call","text":"_generate_typed_rewind_call(pool_expr, types)\n\nGenerate bitmask-aware rewind call. When types are known at compile time, emits a conditional:\n\nif touched types ⊆ tracked types → typed rewind (fast path)\notherwise → _typed_lazy_rewind! (rewinds tracked | touched mask; all touched types have Case A checkpoints via bit 14 lazy mode)\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._get_pool_for_backend-Tuple{Val{:cpu}}","page":"Full API","title":"AdaptiveArrayPools._get_pool_for_backend","text":"_get_pool_for_backend(::Val{:cpu}) -> AdaptiveArrayPool\n\nGet task-local pool for the specified backend.\n\nExtensions add methods for their backends (e.g., Val{:cuda}). Using Val{Symbol} enables compile-time dispatch and full inlining, achieving zero overhead compared to Dict-based registry.\n\nExample (in CUDA extension)\n\n@inline AdaptiveArrayPools._get_pool_for_backend(::Val{:cuda}) = get_task_local_cuda_pool()\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._lazy_checkpoint!-Tuple{AdaptiveArrayPool}","page":"Full API","title":"AdaptiveArrayPools._lazy_checkpoint!","text":"_lazy_checkpoint!(pool::AdaptiveArrayPool)\n\nLightweight checkpoint for lazy mode (use_typed=false macro path).\n\nIncrements _current_depth and pushes bitmask sentinels — but does not save n_active for any fixed-slot typed pool. The _LAZY_MODE_BIT (bit 15) in _touched_type_masks marks this depth as lazy mode so that _record_type_touch! can trigger lazy first-touch checkpoints.\n\nExisting others entries are eagerly checkpointed since there is no per-type tracking for non-fixed-slot pools; Case B in _rewind_typed_pool! handles any new others entries created during the scope (n_active starts at 0 = sentinel).\n\nPerformance: ~2ns vs ~540ns for full checkpoint!.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._lazy_rewind!-Tuple{AdaptiveArrayPool}","page":"Full API","title":"AdaptiveArrayPools._lazy_rewind!","text":"_lazy_rewind!(pool::AdaptiveArrayPool)\n\nComplete rewind for lazy mode (use_typed=false macro path).\n\nReads the combined mask at the current depth, rewinds only the fixed-slot pools whose bits are set, handles any others entries, then pops the depth metadata.\n\nCalled directly from the macro-generated finally clause as a single function call (matching the structure of _lazy_checkpoint! for symmetry and performance).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._looks_like_type-Tuple{Any}","page":"Full API","title":"AdaptiveArrayPools._looks_like_type","text":"_looks_like_type(expr) -> Bool\n\nHeuristic to check if an expression looks like a type. Returns true for: uppercase Symbols (Float64, Int), curly expressions (Vector{T}), GlobalRef to types.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._record_type_touch!-Union{Tuple{T}, Tuple{AbstractArrayPool, Type{T}}} where T","page":"Full API","title":"AdaptiveArrayPools._record_type_touch!","text":"_record_type_touch!(pool::AbstractArrayPool, ::Type{T})\n\nRecord that type T was touched (acquired) at the current checkpoint depth. Called by acquire! and convenience wrappers; macro-transformed calls use _acquire_impl! directly (bypassing this for zero overhead).\n\nFor fixed-slot types, sets the corresponding bit in _touched_type_masks. For non-fixed-slot types, sets _touched_has_others flag.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._reshape_impl!-Union{Tuple{N}, Tuple{M}, Tuple{T}, Tuple{AdaptiveArrayPool, Array{T, M}, NTuple{N, Int64}}} where {T, M, N}","page":"Full API","title":"AdaptiveArrayPools._reshape_impl!","text":"_reshape_impl!(pool::AdaptiveArrayPool, A::Array{T,M}, dims::NTuple{N,Int}) -> Array{T,N}\n\nZero-allocation reshape using setfield!-based wrapper reuse (Julia 1.11+).\n\nSame dimensionality (M == N): setfield!(A, :size, dims) — no pool interaction\nDifferent dimensionality (M ≠ N): Claims a pool slot via _claim_slot!, reuses cached Array{T,N} wrapper with setfield!(:ref, :size) pointing to A's memory. Automatically reclaimed on rewind! via n_active restoration.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._selective_rewind_fixed_slots!-Tuple{AdaptiveArrayPool, UInt16}","page":"Full API","title":"AdaptiveArrayPools._selective_rewind_fixed_slots!","text":"_selective_rewind_fixed_slots!(pool::AdaptiveArrayPool, mask::UInt16)\n\nRewind only the fixed-slot typed pools whose bits are set in mask.\n\nEach of the 8 fixed-slot pools maps to bits 0–7 (same encoding as _fixed_slot_bit). Bits 8–15 (mode flags) are not checked here — callers must strip them before passing the mask (e.g. mask & _TYPE_BITS_MASK).\n\nUnset bits are skipped entirely: for pools that were acquired without a matching checkpoint, _rewind_typed_pool! Case B safely restores from the parent checkpoint.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._store_nd_wrapper!-Tuple{AbstractTypedPool, Int64, Int64, Any}","page":"Full API","title":"AdaptiveArrayPools._store_nd_wrapper!","text":"_store_nd_wrapper!(tp::AbstractTypedPool, N::Int, slot::Int, wrapper)\n\nStore a cached N-D wrapper for the given slot. Creates the per-N Vector if needed.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._tracked_mask_for_types-Tuple{Vararg{Type}}","page":"Full API","title":"AdaptiveArrayPools._tracked_mask_for_types","text":"_tracked_mask_for_types(types::Type...) -> UInt16\n\nCompute compile-time bitmask for the types tracked by a typed checkpoint/rewind. Uses @generated for zero-overhead constant folding.\n\nReturns UInt16(0) when called with no arguments. Non-fixed-slot types contribute UInt16(0) (their bit is 0).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._typed_lazy_checkpoint!-Tuple{AdaptiveArrayPool, Vararg{Type}}","page":"Full API","title":"AdaptiveArrayPools._typed_lazy_checkpoint!","text":"_typed_lazy_checkpoint!(pool::AdaptiveArrayPool, types::Type...)\n\nTyped checkpoint that enables lazy first-touch checkpointing for extra types touched by helpers (use_typed=true, _can_use_typed_path=false path).\n\nCalls checkpoint!(pool, types...) (checkpoints only the statically-known types), then sets _TYPED_LAZY_BIT (bit 14) in _touched_type_masks[depth] to signal typed lazy mode.\n\n_record_type_touch! checks (mask & _MODE_BITS_MASK) != 0 (bit 14 OR bit 15) to trigger a lazy first-touch checkpoint for each extra type on first acquire, ensuring Case A (not Case B) applies at rewind and parent n_active is preserved correctly.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._typed_lazy_rewind!-Tuple{AdaptiveArrayPool, UInt16}","page":"Full API","title":"AdaptiveArrayPools._typed_lazy_rewind!","text":"_typed_lazy_rewind!(pool::AdaptiveArrayPool, tracked_mask::UInt16)\n\nSelective rewind for typed mode (use_typed=true) fallback path.\n\nCalled when _can_use_typed_path returns false (helpers touched types beyond the statically-tracked set). Rewinds only pools whose bits are set in tracked_mask | touched_mask. All touched types have Case A checkpoints, guaranteed by the _TYPED_LAZY_BIT mode set in _typed_lazy_checkpoint!.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._unsafe_acquire_impl!-Union{Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Int64}} where T","page":"Full API","title":"AdaptiveArrayPools._unsafe_acquire_impl!","text":"_unsafe_acquire_impl!(pool, Type{T}, dims...) -> Array{T,N}\n\nInternal implementation of unsafe_acquire!. Called directly by macro-transformed code.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools._uses_local_var-Tuple{Any, Any}","page":"Full API","title":"AdaptiveArrayPools._uses_local_var","text":"_uses_local_var(expr, local_vars) -> Bool\n\nCheck if an expression uses any local variable (recursively). Handles field access (x.y.z) and indexing (x[i]) by checking the base variable.\n\nThis is used to detect cases like acquire!(pool, cp1d.t_i_average) where cp1d is defined locally - the eltype expression can't be evaluated at checkpoint time since cp1d doesn't exist yet.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.acquire!-Tuple{AbstractArrayPool, AbstractArray}","page":"Full API","title":"AdaptiveArrayPools.acquire!","text":"acquire!(pool, x::AbstractArray) -> SubArray\n\nAcquire an array with the same element type and size as x (similar to similar(x)).\n\nExample\n\nA = rand(10, 10)\n@with_pool pool begin\n    B = acquire!(pool, A)  # Same type and size as A\n    B .= A .* 2\nend\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.acquire!-Union{Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Int64}} where T","page":"Full API","title":"AdaptiveArrayPools.acquire!","text":"acquire!(pool, Type{T}, n) -> view type\nacquire!(pool, Type{T}, dims...) -> view type\nacquire!(pool, Type{T}, dims::NTuple{N,Int}) -> view type\n\nAcquire a pooled array of type T with size n or dimensions dims.\n\nReturns a pooled array (backend-dependent type):\n\nCPU 1D: SubArray{T,1,Vector{T},...} (parent is Vector{T})\nCPU N-D: ReshapedArray{T,N,...} (zero creation cost)\nBit (T === Bit): BitVector / BitArray{N} (chunks-sharing, SIMD optimized)\nCUDA: CuArray{T,N} (unified N-way cache)\n\nFor CPU numeric arrays, the return types are StridedArray, compatible with BLAS and broadcasting.\n\nFor type-unspecified paths (struct fields without concrete type parameters), use unsafe_acquire! instead - cached native array instances can be reused.\n\nExample\n\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)      # 1D view\n    m = acquire!(pool, Float64, 10, 10)   # 2D view\n    v .= 1.0\n    m .= 2.0\n    sum(v) + sum(m)\nend\n\nSee also: unsafe_acquire! for native array access.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.acquire_array!","page":"Full API","title":"AdaptiveArrayPools.acquire_array!","text":"acquire_array!(pool, Type{T}, dims...)\n\nAlias for unsafe_acquire!.\n\nExplicit name emphasizing the return type is a raw Array. Use when you prefer symmetric naming with acquire_view!.\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools.acquire_view!","page":"Full API","title":"AdaptiveArrayPools.acquire_view!","text":"acquire_view!(pool, Type{T}, dims...)\n\nAlias for acquire!.\n\nExplicit name emphasizing the return type is a view (SubArray/ReshapedArray), not a raw Array. Use when you prefer symmetric naming with acquire_array!.\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools.checkpoint!-Tuple{AdaptiveArrayPool, Vararg{Type}}","page":"Full API","title":"AdaptiveArrayPools.checkpoint!","text":"checkpoint!(pool::AdaptiveArrayPool, types::Type...)\n\nSave state for multiple specific types. Uses @generated for zero-overhead compile-time unrolling. Increments currentdepth once for all types.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.checkpoint!-Tuple{AdaptiveArrayPool}","page":"Full API","title":"AdaptiveArrayPools.checkpoint!","text":"checkpoint!(pool::AdaptiveArrayPool)\n\nSave the current pool state (n_active counters) to internal stacks.\n\nThis is called automatically by @with_pool and related macros. After warmup, this function has zero allocation.\n\nSee also: rewind!, @with_pool\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.checkpoint!-Union{Tuple{T}, Tuple{AdaptiveArrayPool, Type{T}}} where T","page":"Full API","title":"AdaptiveArrayPools.checkpoint!","text":"checkpoint!(pool::AdaptiveArrayPool, ::Type{T})\n\nSave state for a specific type only. Used by optimized macros that know which types will be used at compile time.\n\nAlso updates currentdepth and bitmask state for type touch tracking.\n\n~77% faster than full checkpoint! when only one type is used.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.default_eltype-Tuple{AbstractArrayPool}","page":"Full API","title":"AdaptiveArrayPools.default_eltype","text":"default_eltype(pool) -> Type\n\nDefault element type for convenience functions when type is not specified. CPU pools default to Float64, CUDA pools to Float32.\n\nBackends can override this to provide appropriate defaults.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.default_eltype-Tuple{DisabledPool{:cpu}}","page":"Full API","title":"AdaptiveArrayPools.default_eltype","text":"default_eltype(::DisabledPool{:cpu}) -> Float64\n\nDefault element type for disabled CPU pools (matches Julia's zeros() default).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.falses!-Union{Tuple{N}, Tuple{AbstractArrayPool, Vararg{Int64, N}}} where N","page":"Full API","title":"AdaptiveArrayPools.falses!","text":"falses!(pool, dims...) -> BitArray\nfalses!(pool, dims::Tuple) -> BitArray\n\nAcquire a bit-packed boolean array filled with false from the pool.\n\nEquivalent to Julia's falses(dims...) but using pooled memory. Uses ~8x less memory than zeros!(pool, Bool, dims...).\n\nExample\n\n@with_pool pool begin\n    bv = falses!(pool, 100)       # BitVector, all false\n    bm = falses!(pool, 10, 10)    # BitMatrix, all false\nend\n\nSee also: trues!, zeros!, acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.foreach_fixed_slot-Union{Tuple{F}, Tuple{F, AdaptiveArrayPool}} where F","page":"Full API","title":"AdaptiveArrayPools.foreach_fixed_slot","text":"foreach_fixed_slot(f, pool::AdaptiveArrayPool)\n\nApply f to each fixed slot TypedPool. Zero allocation via compile-time unrolling.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.get_bitarray!-Union{Tuple{N}, Tuple{AdaptiveArrayPools.BitTypedPool, NTuple{N, Int64}}} where N","page":"Full API","title":"AdaptiveArrayPools.get_bitarray!","text":"get_bitarray!(tp::BitTypedPool, dims::NTuple{N,Int}) -> BitArray{N}\n\nGet a BitArray{N} that shares chunks with the pooled BitVector.\n\nUses setfield!-based wrapper reuse — unlimited dim patterns, 0-alloc after warmup.\n\nImplementation Notes\n\nBitVector (N=1): size() uses len field, dims is ignored\nBitArray{N>1}: size() uses dims field\nAll BitArrays share chunks with the pool's backing BitVector\n\nSafety\n\nThe returned BitArray is only valid within the @with_pool scope. Do NOT use after the scope ends (use-after-free risk).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.get_nd_array!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractTypedPool{T, Vector{T}}, NTuple{N, Int64}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.get_nd_array!","text":"get_nd_array!(tp::AbstractTypedPool{T,Vector{T}}, dims::NTuple{N,Int}) -> Array{T,N}\n\nGet an N-dimensional Array from the pool with setfield!-based wrapper reuse.\n\nUses Julia 1.11+ setfield! to mutate cached Array wrappers in-place:\n\nSame N (dimensionality): setfield!(arr, :size, dims) — 0 allocation\nBacking memory: setfield!(arr, :ref, ...) — always updated, 0 allocation in compiled code\nFirst call per (slot, N): unsafe_wrap once, then cached forever\n\nUnlike the N-way cache (Julia 1.10), this has no eviction limit — unlimited dimension patterns per slot are supported with zero allocation after warmup.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.get_nd_view!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractTypedPool{T, V} where V<:AbstractVector{T}, NTuple{N, Int64}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.get_nd_view!","text":"get_nd_view!(tp::AbstractTypedPool{T}, dims::NTuple{N,Int})\n\nGet an N-dimensional view via reshape (zero creation cost).\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.get_task_local_cuda_pool","page":"Full API","title":"AdaptiveArrayPools.get_task_local_cuda_pool","text":"get_task_local_cuda_pool() -> CuAdaptiveArrayPool\n\nRetrieves (or creates) the CUDA pool for the current Task and current GPU device.\n\nRequires CUDA.jl to be loaded. Throws an error if CUDA extension is not available.\n\nSee also: get_task_local_pool for CPU pools.\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools.get_task_local_cuda_pools","page":"Full API","title":"AdaptiveArrayPools.get_task_local_cuda_pools","text":"get_task_local_cuda_pools() -> Dict{Int, CuAdaptiveArrayPool}\n\nReturns the dictionary of all CUDA pools for the current task (one per device).\n\nRequires CUDA.jl to be loaded. Throws an error if CUDA extension is not available.\n\n\n\n\n\n","category":"function"},{"location":"reference/api/#AdaptiveArrayPools.get_task_local_pool-Tuple{}","page":"Full API","title":"AdaptiveArrayPools.get_task_local_pool","text":"get_task_local_pool() -> AdaptiveArrayPool\n\nRetrieves (or creates) the AdaptiveArrayPool for the current Task.\n\nEach Task gets its own pool instance via task_local_storage(), ensuring thread safety without locks.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.get_view!-Union{Tuple{T}, Tuple{AbstractTypedPool{T, V} where V<:AbstractVector{T}, Int64}} where T","page":"Full API","title":"AdaptiveArrayPools.get_view!","text":"get_view!(tp::AbstractTypedPool{T}, n::Int)\n\nGet a 1D vector view of size n from the typed pool. Returns cached view on hit (zero allocation), creates new on miss.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.ones!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Vararg{Int64, N}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.ones!","text":"ones!(pool, dims...) -> view\nones!(pool, T, dims...) -> view\nones!(pool, dims::Tuple) -> view\nones!(pool, T, dims::Tuple) -> view\n\nAcquire a one-initialized array from the pool.\n\nEquivalent to acquire!(pool, T, dims...) followed by fill!(arr, one(T)). Default element type depends on pool backend (CPU: Float64, CUDA: Float32). See default_eltype.\n\nExample\n\n@with_pool pool begin\n    v = ones!(pool, 100)              # Uses default_eltype(pool)\n    m = ones!(pool, Float32, 10, 10)  # Matrix{Float32} view, all ones\nend\n\nSee also: zeros!, similar!, acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.pool_stats-Tuple{AbstractTypedPool}","page":"Full API","title":"AdaptiveArrayPools.pool_stats","text":"pool_stats(tp::AbstractTypedPool; io::IO=stdout, indent::Int=0, name::String=\"\")\n\nPrint statistics for a TypedPool or BitTypedPool.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.pool_stats-Tuple{AdaptiveArrayPool}","page":"Full API","title":"AdaptiveArrayPools.pool_stats","text":"pool_stats(pool::AdaptiveArrayPool; io::IO=stdout)\n\nPrint detailed statistics about pool usage with colored output.\n\nExample\n\npool = AdaptiveArrayPool()\n@with_pool pool begin\n    v = acquire!(pool, Float64, 100)\n    pool_stats(pool)\nend\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.pool_stats-Tuple{Val{:cpu}}","page":"Full API","title":"AdaptiveArrayPools.pool_stats","text":"pool_stats(:cpu; io::IO=stdout)\n\nPrint statistics for the CPU task-local pool only.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.pool_stats-Tuple{Val{:cuda}}","page":"Full API","title":"AdaptiveArrayPools.pool_stats","text":"pool_stats(:cuda; io::IO=stdout)\n\nPrint statistics for CUDA task-local pools. Requires CUDA.jl to be loaded.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.pool_stats-Tuple{}","page":"Full API","title":"AdaptiveArrayPools.pool_stats","text":"pool_stats(; io::IO=stdout)\n\nPrint statistics for all task-local pools (CPU and CUDA if loaded).\n\nExample\n\n@with_pool begin\n    v = acquire!(pool, Float64, 100)\n    pool_stats()  # Shows all pool stats\nend\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.pooling_enabled-Tuple{AbstractArrayPool}","page":"Full API","title":"AdaptiveArrayPools.pooling_enabled","text":"pooling_enabled(pool) -> Bool\n\nReturns true if pool is an active pool, false if pooling is disabled.\n\nExamples\n\n@maybe_with_pool pool begin\n    if pooling_enabled(pool)\n        # Using pooled memory\n    else\n        # Using standard allocation\n    end\nend\n\nSee also: DisabledPool\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.reset!-Tuple{AbstractTypedPool}","page":"Full API","title":"AdaptiveArrayPools.reset!","text":"reset!(tp::AbstractTypedPool)\n\nReset state without clearing allocated storage. Sets n_active = 0 and restores checkpoint stacks to sentinel state.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.reset!-Tuple{AdaptiveArrayPool, Vararg{Type}}","page":"Full API","title":"AdaptiveArrayPools.reset!","text":"reset!(pool::AdaptiveArrayPool, types::Type...)\n\nReset state for multiple specific types. Uses @generated for zero-overhead compile-time unrolling.\n\nSee also: reset!(::AdaptiveArrayPool), rewind!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.reset!-Tuple{AdaptiveArrayPool}","page":"Full API","title":"AdaptiveArrayPools.reset!","text":"reset!(pool::AdaptiveArrayPool)\n\nReset pool state without clearing allocated storage.\n\nThis function:\n\nResets all n_active counters to 0\nRestores all checkpoint stacks to sentinel state\nResets _current_depth and type touch tracking state\n\nUnlike empty!, this preserves all allocated vectors, views, and N-D arrays for reuse, avoiding reallocation costs.\n\nUse Case\n\nWhen functions that acquire from the pool are called without proper checkpoint!/rewind! management, n_active can grow indefinitely. Use reset! to cleanly restore the pool to its initial state while keeping allocated memory available.\n\nExample\n\npool = AdaptiveArrayPool()\n\n# Some function that acquires without checkpoint management\nfunction compute!(pool)\n    v = acquire!(pool, Float64, 100)\n    # ... use v ...\n    # No rewind! called\nend\n\nfor _ in 1:1000\n    compute!(pool)  # n_active grows each iteration\nend\n\nreset!(pool)  # Restore state, keep allocated memory\n# Now pool.n_active == 0, but vectors are still available for reuse\n\nSee also: empty!, rewind!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.reset!-Union{Tuple{T}, Tuple{AdaptiveArrayPool, Type{T}}} where T","page":"Full API","title":"AdaptiveArrayPools.reset!","text":"reset!(pool::AdaptiveArrayPool, ::Type{T})\n\nReset state for a specific type only. Clears n_active and checkpoint stacks to sentinel state while preserving allocated vectors.\n\nSee also: reset!(::AdaptiveArrayPool), rewind!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.reshape!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArrayPool, AbstractArray{T}, Vararg{Int64, N}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.reshape!","text":"reshape!(pool, A, dims...) -> reshaped array\nreshape!(pool, A, dims::Tuple) -> reshaped array\n\nReshape array A to dimensions dims using the pool's wrapper cache.\n\nThe returned array shares memory with A — mutations are visible in both. The pool provides cached wrapper objects to reduce allocation on repeated calls.\n\nOn Julia 1.11+:\n\nIf ndims(A) == length(dims) (same dimensionality), reshape! mutates A in-place by changing its size. This differs from Base.reshape, which always returns a new wrapper.\nFor cross-dimensional reshapes (ndims(A) != length(dims)), the returned Array wrapper is taken from the pool's internal cache and may be reused after rewind! or pool scope exit.\n\nAs with all pool-backed objects, the reshaped result must not escape the surrounding @with_pool scope.\n\nOn Julia 1.10 and CUDA, falls back to Base.reshape.\n\nThrows DimensionMismatch if prod(dims) != length(A).\n\nExample\n\nA = collect(1.0:12.0)\n@with_pool pool begin\n    B = reshape!(pool, A, 3, 4)   # 12-element vector → 3×4 matrix\n    B[1,1] = 999.0                # A[1] is now 999.0\nend\n\nSee also: acquire!, similar!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.rewind!-Tuple{AdaptiveArrayPool, Vararg{Type}}","page":"Full API","title":"AdaptiveArrayPools.rewind!","text":"rewind!(pool::AdaptiveArrayPool, types::Type...)\n\nRestore state for multiple specific types in reverse order. Decrements currentdepth once after all types are rewound.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.rewind!-Tuple{AdaptiveArrayPool}","page":"Full API","title":"AdaptiveArrayPools.rewind!","text":"rewind!(pool::AdaptiveArrayPool)\n\nRestore the pool state (nactive counters) from internal stacks. Uses _checkpointdepths to accurately determine which entries to pop vs restore.\n\nOnly the counters are restored; allocated memory remains for reuse. Handles touched types by checking checkpointdepths for accurate restoration.\n\nSafety: If called at global scope (depth=1, no pending checkpoints), automatically delegates to reset! to safely clear all n_active counters.\n\nSee also: checkpoint!, reset!, @with_pool\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.rewind!-Union{Tuple{T}, Tuple{AdaptiveArrayPool, Type{T}}} where T","page":"Full API","title":"AdaptiveArrayPools.rewind!","text":"rewind!(pool::AdaptiveArrayPool, ::Type{T})\n\nRestore state for a specific type only. Also updates currentdepth and bitmask state.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.safe_prod-Union{Tuple{NTuple{N, Int64}}, Tuple{N}} where N","page":"Full API","title":"AdaptiveArrayPools.safe_prod","text":"safe_prod(dims::NTuple{N, Int}) -> Int\n\nCompute the product of dimensions with overflow checking.\n\nThrows OverflowError if the product exceeds typemax(Int), preventing memory corruption from integer overflow in unsafe_wrap operations.\n\nRationale\n\nWithout overflow checking, large dimensions like (10^10, 10^10) would wrap around to a small value, causing unsafe_wrap to create an array view that indexes beyond allocated memory.\n\nPerformance\n\nAdds ~0.3-1.2 ns overhead (<1%) compared to unchecked prod(), which is negligible relative to the 100-200 ns cost of the full allocation path.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.set_cache_ways!-Tuple{Int64}","page":"Full API","title":"AdaptiveArrayPools.set_cache_ways!","text":"set_cache_ways!(n::Int)\n\nSet the number of cache ways for N-D array caching. Requires Julia restart to take effect.\n\nHigher values reduce cache eviction but increase memory usage per slot.\n\nArguments\n\nn::Int: Number of cache ways (valid range: 1-16)\n\nExample\n\nusing AdaptiveArrayPools\nAdaptiveArrayPools.set_cache_ways!(8)  # Double the default\n# Restart Julia to apply the change\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.similar!-Tuple{AbstractArrayPool, AbstractArray}","page":"Full API","title":"AdaptiveArrayPools.similar!","text":"similar!(pool, array) -> view\nsimilar!(pool, array, T) -> view\nsimilar!(pool, array, dims...) -> view\nsimilar!(pool, array, T, dims...) -> view\n\nAcquire an uninitialized array from the pool, using a template array for defaults.\n\nsimilar!(pool, A): same element type and size as A\nsimilar!(pool, A, T): element type T, same size as A\nsimilar!(pool, A, dims...): same element type as A, specified dimensions\nsimilar!(pool, A, T, dims...): element type T, specified dimensions\n\nExample\n\nA = rand(10, 10)\n@with_pool pool begin\n    B = similar!(pool, A)              # Same type and size\n    C = similar!(pool, A, Float32)     # Float32, same size\n    D = similar!(pool, A, 5, 5)        # Same type, different size\n    E = similar!(pool, A, Int, 20)     # Int, 1D\nend\n\nSee also: zeros!, ones!, acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.trues!-Union{Tuple{N}, Tuple{AbstractArrayPool, Vararg{Int64, N}}} where N","page":"Full API","title":"AdaptiveArrayPools.trues!","text":"trues!(pool, dims...) -> BitArray\ntrues!(pool, dims::Tuple) -> BitArray\n\nAcquire a bit-packed boolean array filled with true from the pool.\n\nEquivalent to Julia's trues(dims...) but using pooled memory. Uses ~8x less memory than ones!(pool, Bool, dims...).\n\nExample\n\n@with_pool pool begin\n    bv = trues!(pool, 100)        # BitVector, all true\n    bm = trues!(pool, 10, 10)     # BitMatrix, all true\nend\n\nSee also: falses!, ones!, acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.unsafe_acquire!-Tuple{AbstractArrayPool, AbstractArray}","page":"Full API","title":"AdaptiveArrayPools.unsafe_acquire!","text":"unsafe_acquire!(pool, x::AbstractArray) -> Array\n\nAcquire a raw array with the same element type and size as x (similar to similar(x)).\n\nExample\n\nA = rand(10, 10)\n@with_pool pool begin\n    B = unsafe_acquire!(pool, A)  # Matrix{Float64}, same size as A\n    B .= A .* 2\nend\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.unsafe_acquire!-Union{Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Int64}} where T","page":"Full API","title":"AdaptiveArrayPools.unsafe_acquire!","text":"unsafe_acquire!(pool, Type{T}, n) -> backend's native array type\nunsafe_acquire!(pool, Type{T}, dims...) -> backend's native array type\nunsafe_acquire!(pool, Type{T}, dims::NTuple{N,Int}) -> backend's native array type\n\nAcquire a native array backed by pool memory.\n\nReturns the backend's native array type:\n\nCPU: Array{T,N} (via unsafe_wrap)\nBit (T === Bit): BitVector / BitArray{N} (chunks-sharing; equivalent to acquire!)\nCUDA: CuArray{T,N} (via unified view cache)\n\nFor CPU pools, since Array instances are mutable references, cached instances can be returned directly without creating new wrapper objects—ideal for type-unspecified paths. For CUDA pools, this delegates to the same unified N-way cache as acquire!.\n\nSafety Warning\n\nThe returned array is only valid within the @with_pool scope. Using it after the scope ends leads to undefined behavior (use-after-free, data corruption).\n\nDo NOT call resize!, push!, or append! on returned arrays - this causes undefined behavior as the memory is owned by the pool.\n\nWhen to Use\n\nType-unspecified paths: Struct fields without concrete type parameters (e.g., _pooled_chain::PooledChain instead of _pooled_chain::PooledChain{M})\nFFI calls expecting raw pointers\nAPIs that strictly require native array types\n\nAllocation Behavior\n\nCPU: Cache hit 0 bytes, cache miss ~112 bytes (Array header via unsafe_wrap)\nCUDA: Cache hit ~0 bytes, cache miss ~80 bytes (CuArray wrapper creation)\n\nExample\n\n@with_pool pool begin\n    A = unsafe_acquire!(pool, Float64, 100, 100)  # Matrix{Float64} (CPU) or CuMatrix{Float64} (CUDA)\n    B = unsafe_acquire!(pool, Float64, 100, 100)\n    C = similar(A)  # Regular allocation for result\n    mul!(C, A, B)   # BLAS uses A, B directly\nend\n# A and B are INVALID after this point!\n\nSee also: acquire! for view-based access.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.unsafe_ones!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Vararg{Int64, N}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.unsafe_ones!","text":"unsafe_ones!(pool, dims...) -> Array\nunsafe_ones!(pool, T, dims...) -> Array\nunsafe_ones!(pool, dims::Tuple) -> Array\nunsafe_ones!(pool, T, dims::Tuple) -> Array\n\nAcquire a one-initialized raw array (not a view) from the pool.\n\nEquivalent to unsafe_acquire!(pool, T, dims...) followed by fill!(arr, one(T)). Default element type depends on pool backend (CPU: Float64, CUDA: Float32). See default_eltype.\n\nExample\n\n@with_pool pool begin\n    v = unsafe_ones!(pool, 100)              # Uses default_eltype(pool)\n    m = unsafe_ones!(pool, Float32, 10, 10)  # Array{Float32}, all ones\nend\n\nSee also: unsafe_zeros!, ones!, unsafe_acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.unsafe_similar!-Tuple{AbstractArrayPool, AbstractArray}","page":"Full API","title":"AdaptiveArrayPools.unsafe_similar!","text":"unsafe_similar!(pool, array) -> Array\nunsafe_similar!(pool, array, T) -> Array\nunsafe_similar!(pool, array, dims...) -> Array\nunsafe_similar!(pool, array, T, dims...) -> Array\n\nAcquire an uninitialized raw array (not a view) from the pool, using a template array for defaults.\n\nunsafe_similar!(pool, A): same element type and size as A\nunsafe_similar!(pool, A, T): element type T, same size as A\nunsafe_similar!(pool, A, dims...): same element type as A, specified dimensions\nunsafe_similar!(pool, A, T, dims...): element type T, specified dimensions\n\nExample\n\nA = rand(10, 10)\n@with_pool pool begin\n    B = unsafe_similar!(pool, A)              # Same type and size, raw array\n    C = unsafe_similar!(pool, A, Float32)     # Float32, same size\n    D = unsafe_similar!(pool, A, 5, 5)        # Same type, different size\nend\n\nSee also: similar!, unsafe_acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.unsafe_zeros!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Vararg{Int64, N}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.unsafe_zeros!","text":"unsafe_zeros!(pool, dims...) -> Array\nunsafe_zeros!(pool, T, dims...) -> Array\nunsafe_zeros!(pool, dims::Tuple) -> Array\nunsafe_zeros!(pool, T, dims::Tuple) -> Array\n\nAcquire a zero-initialized raw array (not a view) from the pool.\n\nEquivalent to unsafe_acquire!(pool, T, dims...) followed by fill!(arr, zero(T)). Default element type depends on pool backend (CPU: Float64, CUDA: Float32). See default_eltype.\n\nExample\n\n@with_pool pool begin\n    v = unsafe_zeros!(pool, 100)              # Uses default_eltype(pool)\n    m = unsafe_zeros!(pool, Float32, 10, 10)  # Array{Float32}, all zeros\nend\n\nSee also: unsafe_ones!, zeros!, unsafe_acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.zeros!-Union{Tuple{N}, Tuple{T}, Tuple{AbstractArrayPool, Type{T}, Vararg{Int64, N}}} where {T, N}","page":"Full API","title":"AdaptiveArrayPools.zeros!","text":"zeros!(pool, dims...) -> view\nzeros!(pool, T, dims...) -> view\nzeros!(pool, dims::Tuple) -> view\nzeros!(pool, T, dims::Tuple) -> view\n\nAcquire a zero-initialized array from the pool.\n\nEquivalent to acquire!(pool, T, dims...) followed by fill!(arr, zero(T)). Default element type depends on pool backend (CPU: Float64, CUDA: Float32). See default_eltype.\n\nExample\n\n@with_pool pool begin\n    v = zeros!(pool, 100)              # Uses default_eltype(pool)\n    m = zeros!(pool, Float32, 10, 10)  # Matrix{Float32} view, all zeros\nend\n\nSee also: ones!, similar!, acquire!\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#Base.empty!-Tuple{AdaptiveArrayPools.BitTypedPool}","page":"Full API","title":"Base.empty!","text":"empty!(tp::BitTypedPool)\n\nClear all internal storage for BitTypedPool, releasing all memory. Restores sentinel values for 1-based sentinel pattern.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#Base.empty!-Tuple{AdaptiveArrayPools.TypedPool}","page":"Full API","title":"Base.empty!","text":"empty!(tp::TypedPool)\n\nClear all internal storage for TypedPool, releasing all memory. Restores sentinel values for 1-based sentinel pattern.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#Base.empty!-Tuple{AdaptiveArrayPool}","page":"Full API","title":"Base.empty!","text":"empty!(pool::AdaptiveArrayPool)\n\nCompletely clear the pool, releasing all stored vectors and resetting all state.\n\nThis is useful when you want to free memory or start fresh without creating a new pool instance.\n\nExample\n\npool = AdaptiveArrayPool()\nv = acquire!(pool, Float64, 1000)\n# ... use v ...\nempty!(pool)  # Release all memory\n\nWarning\n\nAny SubArrays previously acquired from this pool become invalid after empty!.\n\n\n\n\n\n","category":"method"},{"location":"reference/api/#AdaptiveArrayPools.AbstractArrayPool","page":"Full API","title":"AdaptiveArrayPools.AbstractArrayPool","text":"AbstractArrayPool\n\nAbstract base for multi-type array pools.\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.AbstractTypedPool","page":"Full API","title":"AdaptiveArrayPools.AbstractTypedPool","text":"AbstractTypedPool{T, V<:AbstractVector{T}}\n\nAbstract base for type-specific memory pools.\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.AdaptiveArrayPool","page":"Full API","title":"AdaptiveArrayPools.AdaptiveArrayPool","text":"AdaptiveArrayPool\n\nMulti-type memory pool with fixed slots for common types and IdDict fallback for others. Zero allocation after warmup. NOT thread-safe - use one pool per Task.\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.BackendNotLoadedError","page":"Full API","title":"AdaptiveArrayPools.BackendNotLoadedError","text":"BackendNotLoadedError <: Exception\n\nError thrown when a backend-specific operation is attempted but the backend package is not loaded.\n\nExample\n\n@maybe_with_pool :cuda pool begin\n    zeros!(pool, 10)  # Throws if CUDA.jl not loaded\nend\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.Bit","page":"Full API","title":"AdaptiveArrayPools.Bit","text":"Bit\n\nSentinel type for bit-packed boolean storage via BitVector.\n\nUse Bit instead of Bool in pool operations to get memory-efficient bit-packed arrays (1 bit per element vs 1 byte for Vector{Bool}).\n\nUsage\n\n@with_pool pool begin\n    # BitVector (1 bit per element, ~8x memory savings)\n    bv = acquire!(pool, Bit, 1000)\n\n    # vs Vector{Bool} (1 byte per element)\n    vb = acquire!(pool, Bool, 1000)\n\n    # Convenience functions work too\n    mask = falses!(pool, 100)       # BitVector filled with false\n    flags = trues!(pool, 100)       # BitVector filled with true\nend\n\nReturn Types (Unified for Performance)\n\nUnlike other types, Bit always returns native BitVector/BitArray:\n\n1D: BitVector (both acquire! and unsafe_acquire!)\nN-D: BitArray{N} (reshaped, preserves SIMD optimization)\n\nThis design ensures users always get SIMD-optimized performance without needing to remember which API to use.\n\nPerformance\n\nBitVector operations like count(), sum(), and bitwise operations are ~(10x ~ 100x) faster than equivalent operations on SubArray{Bool} because they use SIMD-optimized algorithms on packed 64-bit chunks.\n\n@with_pool pool begin\n    bv = acquire!(pool, Bit, 10000)\n    fill!(bv, true)\n    count(bv)  # Uses fast SIMD path automatically\nend\n\nMemory Safety\n\nThe returned BitVector shares its internal chunks array with the pool. It is only valid within the @with_pool scope - using it after the scope ends leads to undefined behavior (use-after-free risk).\n\nSee also: trues!, falses!, BitTypedPool\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.BitTypedPool","page":"Full API","title":"AdaptiveArrayPools.BitTypedPool","text":"BitTypedPool <: AbstractTypedPool{Bool, BitVector}\n\nSpecialized pool for BitVector arrays with memory reuse.\n\nUnlike TypedPool{Bool} which stores Vector{Bool} (1 byte per element), this pool stores BitVector (1 bit per element, ~8x memory efficiency).\n\nUnified API (Always Returns BitVector)\n\nUnlike other types, both acquire! and unsafe_acquire! return BitVector for the Bit type. This design ensures users always get SIMD-optimized performance without needing to choose between APIs.\n\nacquire!(pool, Bit, n) → BitVector (SIMD optimized)\nunsafe_acquire!(pool, Bit, n) → BitVector (same behavior)\ntrues!(pool, n) → BitVector filled with true\nfalses!(pool, n) → BitVector filled with false\n\nFields\n\nvectors: Backing BitVector storage\nnd_wrappers: Vector{Union{Nothing, Vector{Any}}} — setfield!-based cache (Julia 1.11+)\nn_active: Count of currently active arrays\n_checkpoint_*: State management stacks (1-based sentinel pattern)\n\nPerformance\n\nOperations like count(), sum(), and bitwise operations are ~(10x ~ 100x) faster than equivalent operations on SubArray{Bool} because BitVector uses SIMD-optimized algorithms on packed 64-bit chunks.\n\nSee also: trues!, falses!, Bit\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.DisabledPool","page":"Full API","title":"AdaptiveArrayPools.DisabledPool","text":"DisabledPool{Backend}\n\nSentinel type for disabled pooling that preserves backend context. When USE_POOLING=false (compile-time) or MAYBE_POOLING_ENABLED[]=false (runtime), macros return DisabledPool{backend}() instead of nothing.\n\nBackend symbols:\n\n:cpu - Standard Julia arrays\n:cuda - CUDA.jl CuArrays (defined in extension)\n\nThis enables @with_pool :cuda to return correct array types even when pooling is off.\n\nExample\n\n# When USE_POOLING=false:\n@with_pool :cuda pool begin\n    v = zeros!(pool, 10)  # Returns CuArray{Float32}, not Array{Float64}!\nend\n\nSee also: pooling_enabled, DISABLED_CPU\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.TypedPool","page":"Full API","title":"AdaptiveArrayPools.TypedPool","text":"TypedPool{T} <: AbstractTypedPool{T, Vector{T}}\n\nInternal structure managing pooled vectors for a specific element type T.\n\nFields\n\nStorage\n\nvectors: Backing Vector{T} storage (actual memory allocation)\n\n1D Cache (for acquire!(pool, T, n))\n\nviews: Cached SubArray views for zero-allocation 1D access\nview_lengths: Cached lengths for fast Int comparison (SoA pattern)\n\nN-D Wrapper Cache (Julia 1.11+, setfield!-based reuse)\n\nnd_wrappers: Vector{Union{Nothing, Vector{Any}}} — indexed by N (dimensionality), each entry is a per-slot cached Array{T,N} wrapper. Uses setfield!(wrapper, :size, dims) and setfield!(wrapper, :ref, parent) for zero-allocation reuse of unlimited dim patterns.\n\nState Management (1-based sentinel pattern)\n\nn_active: Count of currently active (checked-out) arrays\n_checkpoint_n_active: Saved n_active values at each checkpoint (sentinel: [0])\n_checkpoint_depths: Depth of each checkpoint entry (sentinel: [0])\n\nNote\n\nacquire! for N-D returns ReshapedArray (zero creation cost), so no caching needed. unsafe_acquire! uses setfield! wrapper reuse — unlimited dim patterns, 0-alloc after warmup.\n\n\n\n\n\n","category":"type"},{"location":"reference/api/#AdaptiveArrayPools.CACHE_WAYS","page":"Full API","title":"AdaptiveArrayPools.CACHE_WAYS","text":"Number of cache ways per slot for N-way set associative cache. Supports up to CACHE_WAYS different dimension patterns per slot without thrashing.\n\nDefault: 4 (handles most use cases well)\n\nConfiguration\n\nusing AdaptiveArrayPools\nAdaptiveArrayPools.set_cache_ways!(8)  # Restart Julia to take effect\n\nOr manually in LocalPreferences.toml:\n\n[AdaptiveArrayPools]\ncache_ways = 8\n\nValid range: 1-16 (higher values increase memory but reduce eviction)\n\n\n\n\n\n","category":"constant"},{"location":"reference/api/#AdaptiveArrayPools.DISABLED_CPU","page":"Full API","title":"AdaptiveArrayPools.DISABLED_CPU","text":"DISABLED_CPU\n\nSingleton instance for disabled CPU pooling. Used by macros when USE_POOLING=false without backend specification.\n\n\n\n\n\n","category":"constant"},{"location":"reference/api/#AdaptiveArrayPools.FIXED_SLOT_FIELDS","page":"Full API","title":"AdaptiveArrayPools.FIXED_SLOT_FIELDS","text":"FIXED_SLOT_FIELDS\n\nField names for fixed slot TypedPools. Single source of truth for foreach_fixed_slot.\n\nWhen modifying, also update: struct definition, get_typed_pool! dispatches, constructor. Tests verify synchronization automatically.\n\n\n\n\n\n","category":"constant"},{"location":"reference/api/#AdaptiveArrayPools.MAYBE_POOLING_ENABLED","page":"Full API","title":"AdaptiveArrayPools.MAYBE_POOLING_ENABLED","text":"MAYBE_POOLING_ENABLED\n\nRuntime flag for @maybe_with_pool macro only. When false, @maybe_with_pool will use nothing as the pool, causing acquire! to allocate normally.\n\nNote: This only affects @maybe_with_pool. @with_pool ignores this flag (always uses pooling).\n\nFor complete removal of pooling overhead at compile time, use USE_POOLING instead.\n\nDefault: true\n\n\n\n\n\n","category":"constant"},{"location":"reference/api/#AdaptiveArrayPools.POOL_DEBUG","page":"Full API","title":"AdaptiveArrayPools.POOL_DEBUG","text":"POOL_DEBUG\n\nWhen true, @with_pool macros validate that returned values don't reference pool memory (which would be unsafe).\n\nDefault: false\n\n\n\n\n\n","category":"constant"},{"location":"reference/api/#AdaptiveArrayPools.USE_POOLING","page":"Full API","title":"AdaptiveArrayPools.USE_POOLING","text":"USE_POOLING::Bool\n\nCompile-time constant (master switch) to completely disable pooling. When false, all macros (@with_pool, @maybe_with_pool) generate code that uses nothing as the pool, causing acquire! to fall back to normal allocation.\n\nThis enables zero-overhead when pooling is disabled, as the compiler can eliminate all pool-related code paths.\n\nConfiguration via Preferences.jl\n\nSet in your project's LocalPreferences.toml:\n\n[AdaptiveArrayPools]\nuse_pooling = false\n\nOr programmatically (requires restart):\n\nusing Preferences\nPreferences.set_preferences!(AdaptiveArrayPools, \"use_pooling\" => false)\n\nDefault: true\n\n\n\n\n\n","category":"constant"},{"location":"basics/with-pool-patterns/#@with_pool-Patterns","page":"@with_pool Patterns","title":"@with_pool Patterns","text":"The @with_pool macro provides automatic memory lifecycle management. It supports two usage patterns depending on your needs.","category":"section"},{"location":"basics/with-pool-patterns/#Pool-Name:-Choose-Any-Identifier","page":"@with_pool Patterns","title":"Pool Name: Choose Any Identifier","text":"The first argument to @with_pool is a variable name you choose - it doesn't have to be pool:\n\n@with_pool p function foo() ... end\n@with_pool mypool function bar() ... end\n@with_pool scratch function baz() ... end\n\nUse whatever name makes your code clearest.","category":"section"},{"location":"basics/with-pool-patterns/#Pattern-1:-Function-Decorator","page":"@with_pool Patterns","title":"Pattern 1: Function Decorator","text":"Wraps an entire function with pool management:\n\n@with_pool pool function compute(n)\n    A = acquire!(pool, Float64, n, n)\n    B = zeros!(pool, Float64, n)\n    # ... compute ...\n    return sum(A) + sum(B)\nend\n\nresult = compute(100)  # Zero-allocation after warmup\n\nBest for: Functions that exclusively use pooled arrays, hot-path functions.","category":"section"},{"location":"basics/with-pool-patterns/#Pattern-2:-Block-Wrapper","page":"@with_pool Patterns","title":"Pattern 2: Block Wrapper","text":"Wraps only a portion of a function:\n\nfunction process_data(data)\n    n = length(data)\n\n    @with_pool pool begin\n        temp = acquire!(pool, Float64, n)\n        temp .= data .* 2\n        result = sum(temp)\n    end  # temp marked for reuse here\n\n    return result * 1.5\nend\n\nBest for: Functions with mixed allocation needs, gradual adoption.","category":"section"},{"location":"basics/with-pool-patterns/#Pattern-Comparison","page":"@with_pool Patterns","title":"Pattern Comparison","text":"Aspect Function Decorator Block Wrapper\nScope Entire function begin...end block\nSyntax @with_pool pool function ... @with_pool pool begin ... end\nPool lifetime Function start to return Block entry to exit","category":"section"},{"location":"basics/with-pool-patterns/#Common-Mistakes","page":"@with_pool Patterns","title":"Common Mistakes","text":"# WRONG: returning the array itself\n@with_pool pool function bad()\n    v = acquire!(pool, Float64, 100)\n    return v  # v marked for reuse after return!\nend\n\n# CORRECT: return computed values\n@with_pool pool function good()\n    v = acquire!(pool, Float64, 100)\n    return sum(v)  # Scalar result is safe\nend\n\n# CORRECT: return a copy if you need the data\n@with_pool pool function also_good()\n    v = acquire!(pool, Float64, 100)\n    return copy(v)\nend","category":"section"},{"location":"basics/with-pool-patterns/#See-Also","page":"@with_pool Patterns","title":"See Also","text":"Essential API - Core functions for pool operations\nSafety Rules - Important scope rules","category":"section"},{"location":"","page":"Home","title":"Home","text":"(Image: Stable) (Image: Dev) (Image: CI) (Image: codecov) (Image: Aqua QA) (Image: code style: runic)","category":"section"},{"location":"#AdaptiveArrayPools.jl","page":"Home","title":"AdaptiveArrayPools.jl","text":"Zero-allocation temporary arrays for Julia.\n\nA lightweight library that lets you write natural, allocation-style code while automatically reusing memory behind the scenes. Eliminates GC pressure in hot loops without the complexity of manual buffer management.\n\nSupported backends:\n\nCPU — Array, works out of the box\nCUDA — CuArray, loads automatically when CUDA.jl is available","category":"section"},{"location":"#The-Problem","page":"Home","title":"The Problem","text":"In performance-critical code, temporary array allocations inside loops create massive GC pressure:\n\nfunction compute(n)\n    A = rand(n, n)      # allocates\n    B = rand(n, n)      # allocates\n    C = A * B           # allocates\n    return sum(C)\nend\n\nfor i in 1:10_000\n    compute(100)  # ⚠️ 90k allocations, 2.75 GiB, 31% GC time\nend\n\nThe traditional fix—passing pre-allocated buffers—works for simple cases but quickly becomes impractical:\n\nAPI pollution: Every function needs extra buffer arguments, breaking clean interfaces\nNested calls: Buffers must be threaded through entire call stacks, even third-party code\nDynamic shapes: Hard to pre-allocate when array sizes depend on runtime values\nPackage boundaries: You can't easily pass buffers into library functions you don't control","category":"section"},{"location":"#The-Solution","page":"Home","title":"The Solution","text":"Wrap your function with @with_pool and replace allocations with acquire! or convenience functions:\n\nusing AdaptiveArrayPools, LinearAlgebra, Random\n\n@with_pool pool function compute_pooled(n)\n    A = acquire!(pool, Float64, n, n)  # reuses memory from pool\n    B = similar!(pool, A)\n    C = similar!(pool, A)\n\n    rand!(A); rand!(B)\n    mul!(C, A, B)\n    return sum(C)\nend\n\ncompute_pooled(100)  # warmup\nfor i in 1:10_000\n    compute_pooled(100) # ✅ Zero allocations, 0% GC\nend\n\nMetric Standard AdaptiveArrayPools Improvement\nTime 787 ms 525 ms 1.5× faster\nAllocations ⚠️ 90,000 (2.75 GiB) ✅ 0 100% eliminated\nGC Time ⚠️ 31% ✅ 0% No GC pauses\n\nCUDA support: Same API—just use @with_pool :cuda pool. See CUDA Backend.","category":"section"},{"location":"#How-It-Works","page":"Home","title":"How It Works","text":"@with_pool automatically manages memory lifecycle for you:\n\nCheckpoint — Saves current pool state when entering the block\nAcquire — acquire! returns arrays backed by pooled memory\nRewind — When the block ends, all acquired arrays are marked available for reuse\n\nThis automatic checkpoint/rewind cycle is what enables zero allocation on repeated calls. You just write normal-looking code with acquire! instead of constructors.\n\nacquire! usually returns lightweight views (SubArray, ReshapedArray) that work seamlessly with BLAS/LAPACK. For bit-packed boolean masks, use the Bit sentinel type to get native BitVector/BitArray with SIMD-optimized kernels. If you need native Array types (FFI, type constraints), use unsafe_acquire!—see API Reference.\n\nNote: Keeping acquired arrays inside the scope is your responsibility. Return computed values (scalars, copies), not the arrays themselves. See Safety Guide.\n\nThread-safe by design: Each Julia Task gets its own independent pool—no locks needed. See Multi-Threading for patterns.","category":"section"},{"location":"#Convenience-Functions","page":"Home","title":"Convenience Functions","text":"Common initialization patterns have convenience functions:\n\nFunction Equivalent to\nzeros!(pool, 10) acquire! + fill!(0)\nones!(pool, Float32, 3, 3) acquire! + fill!(1)\nsimilar!(pool, A) acquire! matching eltype(A), size(A)\nreshape!(pool, A, 3, 4) Reshape sharing memory, zero-alloc (1.11+)\n\nThese return views like acquire!. For raw Array types, use unsafe_acquire! or its convenience variants (unsafe_zeros!, unsafe_ones!, unsafe_similar!). See API Reference.","category":"section"},{"location":"#Installation","page":"Home","title":"Installation","text":"using Pkg\nPkg.Registry.add(Pkg.RegistrySpec(url=\"https://github.com/ProjectTorreyPines/FuseRegistry.jl.git\"))\nPkg.add(\"AdaptiveArrayPools\")","category":"section"},{"location":"#Documentation","page":"Home","title":"Documentation","text":"Guide Description\nAPI Reference Complete function and macro reference\nCUDA Backend GPU-specific usage and examples\nSafety Guide Scope rules and best practices\nMulti-Threading Task/thread safety patterns\nConfiguration Preferences and cache tuning","category":"section"},{"location":"#License","page":"Home","title":"License","text":"Apache 2.0","category":"section"},{"location":"#Contact","page":"Home","title":"Contact","text":"Min-Gu Yoo (Image: Linkedin) (General Atomics)  yoom@fusion.gat.com","category":"section"}]
}
